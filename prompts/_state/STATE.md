# Stocknify — Current State

> Live snapshot of where the project is. Updated automatically by Claude Code at the end of every prompt run, plus manually by Claude (Chat) after reviews. Read this first at the start of every session.

**Last updated:** 2026-05-22 (Cycle 5-C — File-Handling delete/archive. Five new `Integration` columns + shared `applyPostImportAction` service + 6 connector primitives (3 SFTP + 3 FTP) + worker + manual-import + PATCH + Edit-Page section + Wizard summary + dialog hint. Backend test count 105 → 149.)
**Active phase:** Phase 4 — Batch 5 (SFTP polish + production-review fallout, Phase 1 pre-test)
**Live URL:** https://app.stocknify.app
**API health:** https://api.stocknify.app/v1/health

---

## What's deployed and working

- **Cycle 5-C — File-Handling delete/archive (2026-05-22).** Fourth cycle of Batch 5, full-stack (Schema + Migration + 6 connector primitives + new shared service module + worker + manual-import + PATCH + Edit-Page section + Wizard summary + import-dialog hint + 14+1 i18n keys). Closes S22 from the Batch-5 triage. The SFTP/FTP scheduled and manual import flows no longer leave the source CSV sitting on the remote server forever — each finalised `ImportRun` triggers a cleanup decision per the new Integration fields. **R0 (Schema + migration):** `apps/api/src/db/schema.prisma` gains five fields on `Integration` — `postImportAction` (`'delete' | 'archive'`, default `'archive'`), `archiveSubdir` (`'archive'`), `maxImportRetries` (0–10, default 3), `failedAction` (default `'archive'`), `failedSubdir` (`'failed'`). Migration `20260522190000_integration_post_import_handling` `ALTER TABLE integrations ADD COLUMN … NOT NULL DEFAULT …` — defaults backfill existing rows on the same tick, no separate UPDATE. Constraints enforced at the API+service layer (Zod) rather than the DB so error messages stay precise. **R1+R2 (connector primitives):** `sftp-client.ts` exports `ensureSftpDirectory` (recursive `mkdir`), `moveSftpFile` (`rename`), `deleteSftpFile` (`delete`); `ftp-client.ts` mirrors via `ensureFtpDirectory` (basic-ftp `ensureDir`), `moveFtpFile` (`rename`), `deleteFtpFile` (`remove`). Each opens its own client, runs one op, closes. Errors funnel through `sanitizeConnectionError` so the operator-facing message stays redacted. `OP_TIMEOUT_MS = 30_000` shared across both modules. **R3 (shared service):** `apps/api/src/services/integrations/post-import-action.ts` exports `applyPostImportAction(args)` with the decision tree: `success | partial` → cleanup per `postImportAction`; `manual + failed` → cleanup immediately per `failedAction` (retries=0); `scheduled + failed` → count failed runs of the same filename since last success (lower bound `lastSuccessfulSyncAt`), cleanup only when count EXCEEDS `maxImportRetries`. Archive destination `<source-dir>/<subdir>/<YYYY-MM>/<filename>` with `getUTCFullYear`/`getUTCMonth` (UTC bucket — timezone-independent). Cleanup failures truncated to 200 chars and appended to `ImportRun.errorSummary` via read-modify-write; the audit-trail update is itself wrapped in try/catch so cleanup NEVER throws past the caller. Unknown action injection (`'foo'`) logs warning and returns without calling helpers. **New `apps/api/src/lib/remote-path.ts`** extracts `joinRemotePath` / `baseName` / `dirName` from the worker so service and worker share one helper (POSIX-style paths regardless of server OS). **R4 (worker wiring):** `sftp-import.worker.ts` hoists `let filePath: string | undefined` to the wider scope, introduces `runPostImportCleanup(status)` closure that (a) skips when `filePath` is undefined (connector threw before listing resolved), (b) skips when `status === 'failed' && !isFinalAttempt` (BullMQ retry-burst safety — option (c) per the prompt; matches the existing `Incident`/`consecutiveFailures` gating). Called after `markScheduleAndHealth` on three paths: success-path return, invalid-template early return (a scheduled-failed scenario with `filePath` already resolved), and the catch-all at the bottom. The "no CSV files in directory" early return correctly skips cleanup — `filePath` stays undefined. **R5 (manual-import wiring):** `routes/integrations/sftp-import.ts` `integrationDefaults` select extended with `tenantId` + the five new fields + `lastSuccessfulSyncAt`. `filePath` hoisted; new `runPostImportCleanupManual(status)` closure passes `trigger: 'manual'` so the service branches into the failed-immediately path. Called on both the try-block success-finalise (after `importRun.update`) and the catch-block (after the failed update). Response then re-reads the `ImportRun` via `findUnique` so any `errorSummary` mutation from cleanup-failure reaches the operator in the same HTTP response. **R6 (PATCH surface):** `routes/integrations/index.ts` `updateIntegrationSchema` accepts `postImportAction` / `failedAction` (`z.enum(['delete','archive'])`), `archiveSubdir` / `failedSubdir` (`min(1).max(64).regex(/^[A-Za-z0-9_-]+$/)` — rejects slashes, dots, whitespace, so `../escape` / `archive/nested` / `.hidden` / `with spaces` all 400), `maxImportRetries` (`int().min(0).max(10)`). Persisted via direct `data.field = parsed.data.field` (no Prisma relation gymnastics — these are scalar columns). **R7 (Edit-Page section):** `(dashboard)/integrations/automatic/[id]/page.tsx` new "Nach-Import-Verhalten" section between Mapping and Schedule. Two sub-blocks (`postImportSuccessSubsection` + `postImportFailedSubsection` with a top-border divider). Segmented-button radios (`postImportRadioClass` helper at file bottom, mirrors `credential-form.tsx`'s `cnRadio` — Tailwind-only, no Radix RadioGroup needed for two options). Draft-then-blur inputs for the two subdirs and the retry count (local `useState` strings, seeded from the integration row on every refetch via `useEffect`); blur with unchanged value is a no-op, blur with empty/out-of-range resets the draft. Auto-save via the `useUpdateIntegration` hook through a new `handlePostImportPatch(patch)` helper that wraps subtle `t('saved')` / `t('saveFailed')` toasts and refetches on settle. Subdir input only renders when the corresponding action is `'archive'` — switching to `'delete'` hides the field but doesn't reset the stored value. **R8 (Wizard summary):** `setup-wizard.tsx` `Step5Summary` pulls `tConfig = useTranslations('integrations.sftp.config')` and adds two new `SummaryRow`s (success-handling = `tConfig('actionArchive')`, failed-handling = `${tConfig('actionArchive')} (${tConfig('maxRetriesLabel')}: 3)`). Reflects schema defaults for new installs — the wizard intentionally doesn't expose toggles; the Edit-Page is the configuration surface. **R9 (Import-now dialog hint):** `import-runs-table.tsx` `ImportNowDialog` body gains an italic `manualImportNoRetryNote` paragraph above the footer — explicit operator contract that manual imports run with `maxImportRetries === 0` so a single failed manual import triggers the failed-action immediately. **R10 (hook types):** `use-integrations.ts` `UpdateIntegrationInput` + `IntegrationDetail` extended with the five fields (typed enums for actions). **R11 (i18n):** 14 new keys under `integrations.sftp.config` (`postImportActionSection`, `postImportSuccessSubsection`, `postImportFailedSubsection`, `actionArchive`/`actionDelete`, `archiveSubdirLabel`/`failedSubdirLabel`, `archivePathHint`/`failedPathHint` with `{subdir}` ICU param, `maxRetriesLabel`/`maxRetriesHint`, `manualRetryNote`, `saved`/`saveFailed`) + 1 key under `integrations.sftp.runs.importDialog` (`manualImportNoRetryNote`). EN + DE parity. **R12 (tests):** **+44** backend tests across 5 files. **New** `apps/api/src/integrations/__tests__/file-operations.test.ts` (6 smoke tests pinning each primitive's library call — `vi.hoisted` mock of `ssh2-sftp-client` constructor + `basic-ftp` Client class). **New** `apps/api/src/services/integrations/__tests__/post-import-action.test.ts` (11 tests covering success+archive path math, success+delete, partial=success, manual+failed+archive immediate, manual+failed+delete, scheduled-failed below threshold, scheduled-failed exactly at threshold (`> not ≥`), scheduled-failed above threshold, counter resets after success via `lastSuccessfulSyncAt` lower bound, cleanup-throw → `errorSummary` appended → no exception, unknown action → warning + skip). **Extended** `sftp-import.worker.test.ts` (+3 cleanup-wiring tests: final-attempt with resolved filePath invokes service with `trigger: 'scheduled'`; non-final-attempt does NOT invoke; listing-threw-early/`filePath` undefined does NOT invoke). **Extended** `sftp-import.test.ts` (+2 manual-import tests: successful import → ensure+move called with `<dir>/archive/<YYYY-MM>/<file>`; failed import with `failedAction: 'delete'` → `deleteSftpFile` called immediately on `/exports/data.csv`). **Extended** `integration-config-anchor.test.ts` (+22 PATCH cases via `it.each`: combined-set-of-5 200, enum-invalid 400 ×2, subdir-pattern-invalid 400 ×7 (`../escape`, `with spaces`, `archive/nested`, `.hidden`, `../etc`, `has space`, `''`), subdir-valid 200 (`valid_name-01`), retries-out-of-range 400 ×3 (11, -1, 1.5), retries-boundary 200 ×2 (0, 10)). Existing route tests (`sftp-import.test.ts`) had their connector mock extended to include `ensureSftpDirectory` / `moveSftpFile` / `deleteSftpFile` (and FTP equivalents) as no-op resolved so the service module's cleanup branch never throws on transit calls. **Suite total: 105 → 149** (well above the prompt's 130 target). **Build + typecheck:** `pnpm -C apps/api typecheck` + `lint` (0 errors) + `build` clean; `pnpm -C apps/web typecheck` + `build` (`/integrations/automatic/[id]` first-load JS 6.57 → 7.29 kB) + `lint` (0 errors) clean. **Non-goals respected:** no token-driven custom archive paths (`<YYYY>` / `<filename>` expansion deferred), no `Integration.importPath` (5-E), no FTPS-specific cleanup logic (FTPS rolls up under FTP via the `secure: true` wrappers), no `maxImportRetries > 10`, no skip-cleanup body override on import-now, no `lastProcessedFilename` dedupe (still a documented TODO — the counter mitigates without eliminating cleanup-failure-of-failed-file loops). Result: `prompts/results/RESULT_5-C_FILE_HANDLING.md`. Review classification `review:mandatory` — Codex pass queued after push.
- **Cycle 5-B — Schedule Builder Rebuild (2026-05-22).** Third cycle of Batch 5, frontend-only, no backend touch, no schema. Closes S15/S16 (presets broken), S20 (wizard summary hardcoded English), and S25 (Edit-Page schedule section inherits — automatic with no changes). **R1 (presets):** `apps/web/src/components/integrations/schedule-builder.tsx` `INTERVAL_PRESETS` reshaped from `{ minutes, label }` (pure-minute model with implicit conversion) to `{ scheduleType, intervalValue, labelKey }` so each preset carries its target type explicitly. `setInterval(n)` replaced with `applyPreset(preset)` that sets both `scheduleType` and `intervalValue` atomically — clicking "15 min" while type is `interval_hours` now correctly switches to `interval_minutes`/15 (pre-fix it stayed `interval_hours` and the preview read "every 15 hours"). Preset buttons gained an active-highlight state derived purely from `value.scheduleType === p.scheduleType && value.intervalValue === p.intervalValue` (no `useEffect` — derivation is enough); editing the number-input to a non-preset value clears the highlight naturally. Preset visibility gating is unchanged (presets render only inside the `interval_minutes || interval_hours` block — they're an interval-specific affordance). **R2 (shared helper):** `previewSentence` renamed to `formatSchedulePreview` and exported from `schedule-builder.tsx` — pure rename, function body unchanged, single source of truth. `ScheduleBuilder`'s own preview card now calls `formatSchedulePreview(value, locale, t)`. **R3 (wizard):** `apps/web/src/components/integrations/setup-wizard.tsx` `Step5Summary` deleted the local hardcoded-English `describeSchedule(s)` function and now imports `formatSchedulePreview` + `useLocale` from `next-intl`. `tSchedule = useTranslations('integrations.sftp.scheduleBuilder')` pulls the localised namespace; the schedule SummaryRow reads `formatSchedulePreview(state.schedule, locale, tSchedule)`. **R4 (i18n):** 5 new keys under `integrations.sftp.scheduleBuilder` in both en + de — `preset15min`/`preset30min`/`preset1h`/`preset2h`/`preset4h`. EN keeps "15 min"/"1 h" / DE "15 Min"/"1 Std". Existing `preview*` keys unchanged. **R5 (Edit-Page free):** verified `apps/web/src/app/(dashboard)/integrations/automatic/[id]/page.tsx:573` still renders `<ScheduleBuilder value={scheduleValue} onChange={setScheduleValue} />` post-5-A.5 — R1 + R2 propagate automatically. **Non-goals respected:** no new schedule types, no S17 multi-time-of-day, no backend changes, no timezone picker, no wizard step refactor. **Build & typecheck:** `pnpm -C apps/web typecheck` clean, `pnpm -C apps/web build` green; `/integrations/automatic/[id]` first-load JS 5.98 → 6.57 kB (helper export + `useLocale` import), `/integrations/automatic` 5.46 → 5.37 kB. Lint: 0 errors, 3 pre-existing warnings on untouched files. Result: `prompts/results/RESULT_5-B_SCHEDULE_BUILDER_REBUILD.md`. Review classification `review:skip` so no Codex pass. NEXT.md S15, S16, S20, S25 closed; Batch-5 Phase 1 cycles 5-A + 5-A.5 + 5-B all done; next cycle 5-C (File-Handling).
- **Cycle 5-A.5 Codex review fixes (2026-05-22) — both findings ACTIONABLE, fixed in-session.** `/codex:adversarial-review --base origin/main` returned a `needs-attention` verdict on the cycle's diff with two findings, both fixed before merge. **[high] F1 — Credential DELETE precheck filtered `deletedAt: null` on the new Integration-reference count, but the FK on `integrations.credential_id` is `ON DELETE RESTRICT` regardless of soft-delete state:** a soft-deleted integration still holding the FK would slip the app-level guard (count = 0), the code would proceed to the credential delete, and the DB-level FK rejection would surface as P2003 / 500 instead of the deterministic 409 the route promises. **Fix:** `apps/api/src/routes/credentials/index.ts` — Integration-reference count inside the SERIALIZABLE transaction (Cycle 3-B) drops the `deletedAt: null` filter; the schedule-side count is unchanged because the schedule FK is `ON DELETE SET NULL` (different semantics, different guard shape). Added a P2003 catch as defence-in-depth that maps to the same 409. **Regression test:** `credentials.test.ts` gains a test that soft-deletes the referencing integration after wiring its `credentialId`, then attempts the credential DELETE — must return 409, credential must persist. Fails pre-fix (500), passes post-fix. **[medium] F2 — Migration backfill SQL filtered only `deleted_at IS NULL`, ignoring `is_active`:** an integration with a paused-but-not-deleted schedule alongside an active schedule could promote the inactive schedule's credential / mapping into the Integration defaults if the inactive row was newer by `created_at`. Subsequent imports would silently run against the wrong credential. **Fix:** `apps/api/src/db/migrations/20260522155500_integration_config_anchor/migration.sql` — inner SELECT now filters `WHERE deleted_at IS NULL AND is_active = TRUE`, ORDER BY adds `id DESC` as a deterministic tie-breaker. Migration SQL was edited in place (safe because nothing has been deployed to production yet — `develop → main` merge gate is still pending), test DBs reset via `prisma migrate reset` to pick up the new checksum. **Regression test:** `integration-config-anchor.test.ts` gains a test that seeds an integration with an OLDER active schedule (active-cred) and a NEWER inactive schedule (inactive-cred), runs the fixed backfill SQL inline via `$executeRawUnsafe`, and asserts `integration.credentialId === activeCredential.id`. Backend test suite: 109 → 111. No DEFERRED findings. Disposition: `prompts/results/REVIEW_5-A.5_INTEGRATION_CONFIG_ANCHOR.md`.
- **Cycle 5-A.5 — Integration als Konfig-Anker (2026-05-22).** Second cycle of Batch 5, full-stack (Schema + Migration + 4 backend routes + worker + frontend Edit-Page + Wizard). The data model that coupled WIE (credential, mapping) and WANN (cron / time-of-day) on `IntegrationSchedule` is now split: `Integration` gains `credentialId` + `csvMappingTemplateId` as authoritative defaults, schedules keep both columns as nullable override slots (no UI exposes them today). **R1 (Schema + Migration):** `apps/api/src/db/schema.prisma` gets two nullable FK columns on `Integration` with `onDelete: Restrict` + named inverse relations (`IntegrationDefaultCredential` / `IntegrationDefaultMappingTemplate`) on `IntegrationCredential` / `CsvMappingTemplate` (Prisma disambiguates against the existing Schedule-side relations). New indexes on `integrations.credential_id` + `integrations.csv_mapping_template_id`. Migration `20260522155500_integration_config_anchor` adds columns + FKs + indexes + backfills from the newest active schedule per integration via `DISTINCT ON (integration_id) ORDER BY created_at DESC`. `integration_schedules.credential_id` was already nullable since init-v3 — no `ALTER COLUMN` needed and the migration note states this. **R2 (PATCH Integration):** `updateIntegrationSchema` accepts `credentialId`/`csvMappingTemplateId` as `z.string().uuid().nullable().optional()`. New `RENAMABLE_MARKETPLACE_KEYS = {sftp,ftp,ftps}` narrows the existing "name immutable on marketplace integrations" 400 — SFTP-family marketplace integrations can now be renamed; Shopify/Hive/Byrd still reject. `config` immutability stays for ALL marketplace. Credential validation: must be tenant-bound, active, SFTP-family `credentialType`, and either reusable (`integrationId = null`) or already bound to this integration; otherwise `400 INVALID_CREDENTIAL` with specific message. Mapping validation: must be `direction='import'` + `resourceType='stock'`; otherwise `400 INVALID_MAPPING_TEMPLATE`. **R3 (DELETE Credential):** the existing SERIALIZABLE transaction (Cycle 3-B fix) now sums Schedule + Integration references inside the same transaction; 409 message rephrased to "active reference(s)" to cover both paths. **R4 (Schedule POST/PATCH):** `credentialId` Zod becomes optional on POST; handler validates when present, falls back to `Integration.credentialId` when omitted, returns `400 CREDENTIAL_NOT_CONFIGURED` with a specific message when both are null. DB write stores `body.data.credentialId ?? null` so the schedule slot stays empty when the request inherits (worker resolves at run time). PATCH: `credentialId` becomes `string | null`; explicit `null` clears the override. **R5 (Manual import):** `POST /v1/integrations/:id/import-now` `credentialId` becomes optional; handler loads `Integration.credentialId` + `csvMappingTemplateId` first and applies body overrides; missing both → `400 CREDENTIAL_NOT_CONFIGURED`. **R6 (Worker):** `processSftpImportJob` schedule loader now `include`s `integration.credential` + `integration.csvMappingTemplate`. Credential = `schedule.credential ?? integration.credential` (override wins, default as fallback); same for mapping. Downstream code unchanged because both relations resolve to identical record shapes. **R7 (Frontend name inline-edit):** `apps/web/src/app/(dashboard)/integrations/automatic/[id]/page.tsx` PageHeader title becomes a `ReactNode` (the prop type was widened on `PageHeader` for this) with Pencil icon → Input swap. Enter / explicit Check button / Blur all commit via `useUpdateIntegration({ name })`; Escape / X-button cancel without saving; empty-after-trim or unchanged → no save, revert. Loader2 inline while pending. **R8 (Credential + Mapping sections):** Credential + Mapping are now top-level sections that read `integration.credentialId` / `integration.csvMappingTemplateId` directly and auto-save on selector change via `useUpdateIntegration`. Subtle spinner in the section header during pending; success toast on settle; failure toast + `integrationQuery.refetch()` to roll back the optimistic state. Schedule section shrinks to time-only fields (ScheduleBuilder + active toggle + Save/Delete + next-run timestamp); the schedule save handler no longer sends `credentialId`/`csvMappingTemplateId`. Schedule Save button `disabled` when `integration.credentialId` is null AND `handleSaveSchedule` short-circuits with `needCredentialForSchedule` toast if it ever fires defensively. **R9 (Wizard wire-up):** `setup-wizard.tsx` submit handler now PATCHes the freshly-installed integration with `{ credentialId, csvMappingTemplateId }` via `useUpdateIntegration.mutateAsync` after install. The schedule POST body no longer carries credential/mapping; backend fallback resolves them. **R10 (`useUpdateIntegration` hook):** new generic mutation on `apps/web/src/lib/api/use-integrations.ts` accepting `{ id, name?, isEnabled?, credentialId?, csvMappingTemplateId? }`. Invalidates `['integration', id]`, `['integrations-list']`, `['marketplace-catalog']` on settle so all surfaces converge. `IntegrationDetail` gains the two new fields. `useToggleIntegration` left in place as a named export; not refactored to delegate (would change call-site shapes for no win). **R11 (i18n):** 13 new keys in en + de under `integrations.sftp.config` covering inline-edit (`editName`/`saveName`/`cancelEdit`/`nameSaved`/`nameSaveFailed`), auto-save toasts (`credentialSaved`/`credentialSaveFailed`/`mappingSaved`/`mappingSaveFailed`), schedule gating (`needCredentialForSchedule`), and small status hints (`savingShort`/`savedShort`). **R12 (Tests):** +14 backend tests across `integration-config-anchor.test.ts` (new file, 6 tests on the PATCH surface), `credentials.test.ts` (+1 for Integration-only reference 409), `schedules.test.ts` (+2 for credential fallback), `sftp-import.test.ts` (+2 for manual-import fallback), `sftp-import.worker.test.ts` (+2 for worker fallback). Suite total: **95 → 109**, target was ≥105. **Build + typecheck:** `pnpm -C apps/api typecheck` + `lint` + `build` clean; `pnpm -C apps/web typecheck` + `lint` + `build` clean. **Non-goals respected:** no Schedule-Override UI (schema preserves the columns), no Wizard step refactor, no `Integration.importPath` (5-E), no `postImportAction`/`archivePath` (5-C), no non-SFTP marketplace rename. Result: `prompts/results/RESULT_5-A.5_INTEGRATION_CONFIG_ANCHOR.md`. Review classification `review:mandatory` — Codex pass queued after push.
- **Cycle 5-A — Ghost Integration Fix + Delete-Konsistenz (2026-05-13).** First cycle of Batch 5, frontend-only, no backend touch. Closes three findings from Sebastian's Batch-4 walk-through (S1/S2/S27). **R1 (Ghost-fix):** the `AddMethodDialog` on `apps/web/src/app/(dashboard)/integrations/automatic/page.tsx` no longer owns the install mutation. Clicking "Direkte Konfiguration" now closes the chooser and opens a new inline `SftpNameDialog` (defined in the same file, mirroring the existing `AddMethodDialog` co-location pattern). The user types a name (or leaves the field empty, where the placeholder shows "SFTP Import"), and only the explicit Erstellen press fires `install.mutate({ key: 'sftp', name: trimmed || 'SFTP Import' }, …)` — followed by `router.push('/integrations/automatic/' + result.integration.id)` on success. Form-wrapped so Enter submits. Cancel is a no-op. Pre-fix behaviour created a ghost row on every direct-config click; this fix preserves the empty backend state across cancel. **R2 (Card dropdown):** `AutomaticCard` gained a three-dot dropdown menu (`MoreVertical` icon Button → `DropdownMenu` → single `DropdownMenuItem` with `text-destructive focus:text-destructive` for "Löschen") clustered with the existing Switch in a `flex items-center gap-2 flex-shrink-0` group at the right edge of the header row. The dropdown opens a confirm `Dialog` (`max-w-sm`) titled "Integration löschen?" / "Delete integration?" with a description that explains the cascade ("…und der zugehörige Zeitplan werden gelöscht. Zugangsdaten bleiben erhalten…"). On confirm: `uninstall.mutateAsync(integration.id)` → success toast (`deleteSuccess` with the integration name) → close dialog. Failure: destructive toast (`deleteFailed`), dialog stays open. The card disappears from the grid when `useIntegrationsList` refetches via the new invalidation. **R3 (Edit-page header delete):** `apps/web/src/app/(dashboard)/integrations/automatic/[id]/page.tsx` gained the same three-dot dropdown to the right of the existing `Switch` in the PageHeader and an identical confirm dialog. Success → `router.push('/integrations/automatic')`. Re-uses the `integrations.sftp.list.*` namespace for the delete copy (single source of truth shared with the card flow, per prompt option (b)). **R4 (Cache invalidation):** `apps/web/src/lib/api/use-integrations.ts` `useUninstallIntegration.onSettled` now invalidates both `['marketplace-catalog']` and `['integrations-list']` so both surfaces converge after a delete. Marketplace cards still call this hook and still get the catalog refresh. **R5 (i18n):** 14 new keys under `integrations.sftp.list` in en + de (`directNameDialog{Title,Description}`, `directName{Label,Placeholder,Create,Creating}`, `card{Actions,Delete}`, `deleteConfirm{Title,Description}`, `deleteConfirm`, `deleting`, `deleteSuccess`, `deleteFailed`). **Non-goals respected:** no backend change (`DELETE /v1/integrations/:id` from Cycle 4-A handles cascade; install endpoint unchanged), no marketplace-card edits (already had this pattern), no "Bearbeiten" dropdown item (card name + Konfigurieren link already navigate to edit). **Build & typecheck:** `pnpm -C apps/web typecheck` clean, `pnpm -C apps/web build` green. `/integrations/automatic` first-load JS 5.01 → 5.46 kB (Input/Label/Dialog/DropdownMenu primitives + name dialog); `/integrations/automatic/[id]` 5.81 → 5.98 kB (Dialog + DropdownMenu for the header). Lint: 0 errors, 3 pre-existing warnings on untouched files. Result: `prompts/results/RESULT_5-A_GHOST_INTEGRATION_DELETE.md`. Review classification `review:skip` (purely UX, no data-flow / auth / backend surface) so no Codex pass. NEXT.md S1, S2, S27 closed.
- **Cycle 4-E Codex stop-time review (2026-05-12) — both findings ACTIONABLE, fixed in-session.** `/codex:adversarial-review --base origin/main` returned a `needs-attention` verdict with one `[high]` correctness finding + one `[medium]` security finding on the 4-E delta. **[high] F1 — Empty Set on a chart dropdown silently widened the table fetch back to "no filter for that dimension":** the URL parser preserves Clear-all as `new Set()` (Cycle 2-E review-fix made the parse symmetric), but the new `tableFilters` `toCsv` collapsed empty Set → `undefined`, which `toQueryString` stripped, which the backend reads as "no filter applied" → table widens to every movement in range instead of zero. The chart already short-circuits to a `noFilterSelected` hint card when this state is reached; the table did not. **Fix R1:** `apps/web/src/lib/api/use-stock-movements.ts` `useStockMovements` gained an optional `{ enabled?: boolean }` second arg (defaults to `true`) that defers to TanStack's `useQuery({ enabled })`. The `noFilterSelected` computation in `apps/web/src/app/(dashboard)/stock/movements/page.tsx` was hoisted above the `useStockMovements` calls so the table fetch gates on `{ enabled: !noFilterSelected }`. The table `<section>` now branches on `noFilterSelected` and renders the same hint card as the chart — both halves of the page agree on every cell of (selection × visible-data). Codex's recommended fix was a server-side sentinel; I went with a frontend gate for symmetry with the chart's existing behaviour, no new backend semantics, and TanStack-idiomatic state-gating (full rationale in `REVIEW_4-E_TABLE_FILTER_SYNC.md`). **[medium] F2 — Unbounded CSV filter lists enabled query amplification / DoS:** the three plural params accepted arbitrary-length CSV strings (no schema cap, no item cap, no dedupe) and dropped them straight into Prisma `{ in: [...] }`. A caller submitting `?locationIds=A,A,A,…` × 100k could pin Postgres planner cost / blow through prepared-statement parameter ceilings. **Fix R2:** three-layer defence in `apps/api/src/routes/stock/index.ts`. Schema: each plural is now `z.string().max(8192).optional()` (8 kB defence-in-depth ceiling before any allocation). Handler: new `MAX_CSV_FILTER_ITEMS = 100` constant, post-`parseCsvParam` check returns `400 VALIDATION_ERROR` with `"Filter list exceeds maximum of 100 items"` for over-cap requests. Parser: `parseCsvParam` now dedupes via `Array.from(new Set(parts))` so duplicate UUIDs collapse before hitting the `IN` clause. **Tests:** 3 new in `apps/api/src/routes/stock/__tests__/movements.test.ts` — 101 UUIDs in `locationIds` → 400 with the cap message, 8193-byte `stockTypes` string → 400 from the Zod schema cap, `locationIds=A,A,A` returns one row (dedupe pin). Backend suite 92 → 95 green; `pnpm -C apps/api typecheck` + `pnpm -C apps/web typecheck` clean. Review notes in `prompts/results/REVIEW_4-E_TABLE_FILTER_SYNC.md`.
- **Cycle 4-E — Movements-Tabelle: Filter-Sync mit Chart-Dropdowns (2026-05-12).** Fifth cycle of Batch 4, full-stack thin slice (backend + frontend), addresses NEXT.md S9 ("Movements-Filter wirken nur auf Chart, nicht auf Tabelle"). Before 4-E, the chart filter dropdowns (Lager / Lagerplatz / Bestandstyp) only affected the chart; the table was frozen to the legacy single-value URL params from the entry-point deep link — operators changing a dropdown saw the chart narrow but the table stayed wide open. **R1 (backend):** `apps/api/src/routes/stock/index.ts` `movementsQuerySchema` gained three plural CSV params — `locationIds`/`storageLocationIds`/`stockTypes` (each `z.string().optional()`). New `parseCsvParam(raw)` helper splits on `,`, trims, drops empties, returns `string[] | undefined`. The handler resolves plurals first, then falls back to singular: `parseCsvParam(rawLocationIds) ?? (locationId ? [locationId] : undefined)` — **plural wins over singular** when both are sent (back-compat with stock-list deep links carrying the legacy form). Invalid UUIDs in either ID list short-circuit to `400 VALIDATION_ERROR`; `stockTypes` is free-form (not UUID-validated — column-type validation on the Prisma side handles shape). Prisma `where` clause switched from `locationId: value` → `locationId: { in: [...] }` (same shape for `storageLocationId` and `stockType`). **R2 (frontend):** `apps/web/src/lib/api/use-stock-movements.ts` `StockMovementsFilters` interface gains the three plural fields; the hook builds its URL via `toQueryString(filters)` so empty values are filtered out — no other wiring needed. `apps/web/src/app/(dashboard)/stock/movements/page.tsx` `tableFilters` rebuilt from `selectedLocations`/`selectedStorageLocations`/`selectedStockTypes` via an inline `toCsv` helper (`'all'` → undefined, empty Set → undefined, otherwise comma-joined). The three filter-change callbacks now `setPage(1)` so pagination resets when a filter narrows. `chartFilters` deliberately unchanged — the chart's pristine/broad fetch logic from Cycle 4-D stays untouched and the multi-select filtering happens client-side via `filteredSeries` (prevents re-introducing the silent-truncation bug Codex flagged on 2026-05-06). **Non-goals respected:** legend clicks still only affect the chart (filteredSeries is a client-side projection of allSeries, never feeds the table fetch); legacy single-value URL params still carried in the URL for shared-link back-compat (Cycle 4-D's mount-migration effect rewrites them into multi-select form on first commit, so `selectedLocations` carries the entry-point value from tick zero and the table narrows on the original trio without re-reading the legacy params). **Tests:** 4 new in `apps/api/src/routes/stock/__tests__/movements.test.ts` — CSV `locationIds` (A+B together vs A alone), CSV `stockTypes` (available+reserved vs available alone), singular `locationId` back-compat, plural-wins precedence (`?locationId=A&locationIds=A,B` → A+B). Backend test suite 88 → 92 green. `pnpm -C apps/api typecheck` + `pnpm -C apps/web typecheck` green. Result: `prompts/results/RESULT_4-E_TABLE_FILTER_SYNC.md`. Review classification `review:recommended` (backend query-param contract change with multi-tenant SQL surface) — Codex pass queued. KNOWN_TODOS: the stale "Movements page: chart filter and table can disagree (Cycle 2-E fallout)" frontend entry is now closed and removed.
- **Cycle 4-D — Movements-Filter: URL-Sync race + tenant-weite Optionen (2026-05-12).** Fourth cycle of Batch 4, frontend-only, no backend touch. Third pass on the movements-page filter dropdowns (3-A and 4-B each missed a root cause). **Bug 1 (URL-sync race):** for a deep-link `?variantId=X&locationId=abc&stockType=reserved`, the `useState` initialisers correctly seeded `selectedLocations = Set(['abc'])` from the legacy `locationId` URL param. The existing URL-sync `useEffect` then ran on the same commit, read `search.get('locations')` (null — the URL has the legacy param, not the multi-select one) with `null` fallback, got `parseFilterParam(null, null) → 'all'`, and overwrote the initial state — collapsing the dropdown label back to "Alle" on the very first post-mount tick. **Fix R1:** new mount-only `useEffect` placed BEFORE the URL-sync effect (declaration order = fire order within the same commit) that rewrites legacy single-value params into multi-select form (`?locationId=abc → ?locationId=abc&locations=abc`). After this, the sync effect reads the multi-select param it expects and the round-trip is a no-op. The legacy params are kept in the URL so the table (which still consumes the legacy single-value tuple — Cycle 2-E non-goal) keeps narrowing on the entry-point trio. **Bug 2 (option universe = movement data):** Cycle 4-B already decoupled options from `chartRows` by adding a separate `optionsFilters` `useStockMovements` call without per-row narrowing, but options still came FROM movement data — so a product with movements at only one warehouse still showed exactly one option in the location dropdown. Operators couldn't broaden beyond what already existed. **Fix R2:** replaced the entire 4-B options machinery (`optionsFilters` / `useStockMovements(optionsFilters)` / `optionsRows` / `OPTIONS_PER_PAGE`) with tenant-wide hooks: `useLocations()`, `useStorageLocations()`, `useStockTypes()`. All three were already in the bundle (used by the stock page); the movements page now shares the cache. `locationOptions` / `allStorageOptions` / `stockTypeOptions` derive directly from those hooks. **Cascade preserved:** `useStorageLocations()` returns `locationId` on each row, so `parentLocationId`-by-bin metadata still feeds `pruneStorageForLocations` and `filteredStorageOptions`. **`hasScope` short-circuit removed** from the option `useMemo` deps — option precomputation is cheap and the empty-state UI gating on `hasScope` lives at the `MovementFilters` render site. **`pristineEntry` chart optimisation untouched** — the chart still narrows to the legacy trio in pristine mode (Codex 2026-05-06 [high] reason); only OPTIONS broadened, not the chart fetch. **Non-goals respected:** no backend changes, no SingleFilter auto-collapse reintroduction (4-B's removal stays), no table filter changes. **Build & typecheck:** `pnpm -C apps/web typecheck` + `build` green. `/stock/movements` first-load JS dropped from 111 kB to 108 kB (one less `useStockMovements` call; the tenant-wide hooks were already loaded by the stock page). Result: `prompts/results/RESULT_4-D_FILTER_FIX.md`. Review classification `review:skip` (UX/state-machine fix, no data-flow surface) so no Codex pass. KNOWN_TODOS entry updated: the 4-B "options fetch capped at 200 rows" residual is now retired; the new trade-off (every tenant-wide dimension visible even if it has no movements for the current product) is documented as a deliberate MVP choice with a `GET /stock/movement-dimensions` follow-up sketch for the "hide empty dimensions" toggle if it's ever requested.
- **Cycle 4-C — SFTP UI polish: button placement + add-method chooser (2026-05-12).** Third cycle of Batch 4, frontend-only, no backend touch. Closes F4 + F5 from Sebastian's Batch 3 production review. **R1 (button position):** the "Integration hinzufügen" button on `/integrations/automatic` no longer lives inside `<PageHeader>` (sticky bar). It now sits in a section row directly below the header, paired with a new `activeTitle` heading — matches the Marketplace page pattern (`<PageHeader title=… />` + `flex justify-between` row with `<h2>` + `<Button>`). **R2 (add-method chooser):** clicking the button (or the empty-state "Erste Integration anlegen" button) opens a small `Dialog` (`max-w-sm`) with two clickable cards. Card 1 — `Wand2` icon, "Assistent / Setup Wizard" — closes the chooser and opens the existing `SetupWizard`. Card 2 — `SlidersHorizontal` icon, "Direkte Konfiguration / Direct Configuration" — calls `useInstallIntegration({ key: 'sftp', name: 'SFTP Import' })` and on success `router.push(\`/integrations/automatic/\${result.integration.id}\`)`; the icon swaps to `Loader2.animate-spin` while `install.isPending` and both cards disable. Failure surfaces a destructive toast via the new `addDirectFailed` i18n key. Both cards live as a sibling component (`AddMethodDialog`) inside the same `page.tsx` file — keeps the call site readable, no separate file warranted for a two-button dialog. **i18n:** 7 new keys under `integrations.sftp.list` (`activeTitle`, `addMethodTitle`, `addMethodWizard`, `addMethodWizardDescription`, `addMethodDirect`, `addMethodDirectDescription`, `addDirectFailed`) in en + de. **Non-goals respected:** no wizard-step changes, no config-page layout changes, no backend changes. **Build & typecheck:** `pnpm -C apps/web typecheck` + `build` green. `/integrations/automatic` first-load JS rose from ~4 kB to 5.01 kB (246 kB total) — new dialog + two extra Lucide icons. Result: `prompts/results/RESULT_4-C_SFTP_UI_POLISH.md`. Review classification `review:skip` (purely UX, no data-flow surface) so no Codex pass.
- **Cycle 4-B — Movements filter options decoupled from chart fetch (2026-05-12).** Second cycle of Batch 4, frontend-only, no backend touch. Closes F3 from Sebastian's Batch 3 production review: deep-linked entries (`/stock/movements?productId=X&locationId=A&storageLocationId=A1&stockType=available`) used to render each filter dropdown with exactly one option — because option derivation read from `chartRows`, which in pristine entry mode had the API call narrowed to the entry-point trio. The trio-of-one then collapsed via `if (current.size === options.length) onChange('all')` → label said "Alle" even though one concrete row was the pre-selection. **Fix R1:** new `optionsFilters` `useStockMovements` call in `apps/web/src/app/(dashboard)/stock/movements/page.tsx` — same product/variant + date range as the chart, but never narrowed by locationId/storageLocationId/stockType. `locationOptions`/`allStorageOptions`/`stockTypeOptions` now derive from `optionsRows` (this call's `data`) instead of `chartRows`. The chart's own pristine-entry narrowing stays untouched (Codex 2026-05-06 [high] reason). `OPTIONS_PER_PAGE = 200` matches the backend `movementsQuerySchema.perPage.max(200)` cap; the prompt's `perPage: 500` example would have 400'd. **Fix R2:** removed the `current.size === options.length` auto-collapse in `apps/web/src/components/stock/movement-filters.tsx` `handleToggle`. The "Select all" button still sets the `'all'` sentinel explicitly when the operator actually wants it; manual ticking of every box no longer silently rewrites itself back. **Option chosen:** Option A (separate movements fetch) over Option B (existing `useLocations`/`useStorageLocations`/`useStockTypes` hooks) — product/variant-scoped options match operator intent better than tenant-wide lists with empty-result selections. **Acceptance criteria pinned:** dropdowns show all available options for the product/variant; deep-linked values pre-selected and visible in label; "Alle" only on explicit Select-all. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS unchanged at 111 kB. Residual structural cap (200-row options window) updated in `KNOWN_TODOS.md`. Result: `prompts/results/RESULT_4-B_MOVEMENTS_FILTER_FIX.md`.
- **Cycle 4-A Codex stop-time review (2026-05-12) — all 3 findings ACTIONABLE, fixed in-session.** Codex's adversarial review of `origin/main...HEAD` flagged three real correctness/data-integrity issues in the marketplace fix. **[high] F1 — Install path can duplicate locked templates under concurrent first installs:** `existingLocked` was read outside the transaction so two concurrent first installs of the same `(tenant, marketplace_key)` could both observe "no locked templates" and both create them. **Fix:** install handler now runs the integration create + locked-template check/create inside one `Prisma.TransactionIsolationLevel.Serializable` transaction with bounded retry on `P2034` (mirrors the credential-delete pattern in `credentials/index.ts:404-455`); on retry exhaustion → `503 SERIALIZATION_FAILED`. `P2002` is also retried once as belt-and-suspenders. A new partial unique index `csv_mapping_templates_locked_unique` on `(tenant_id, marketplace_key, name) WHERE is_locked = true AND deleted_at IS NULL` lives in `apps/api/src/db/sql/unique-locked-mapping-templates.sql` and is registered in the manual-migration sweep right after the v6b drop — the DB-level safety net stands even if isolation is downgraded in a future change. **[high] F2 — Per-instance delete race could leave active installs without locked templates:** `findFirst` + sibling count + write were three separate Prisma calls, so a delete racing with a concurrent install could decide "siblings == 0, tear down templates" while the install skipped template creation. **Fix:** DELETE handler now runs the full read+write under `SERIALIZABLE` with `P2034` retry; PostgreSQL's predicate locks abort one transaction when install+delete touch the same key bucket. Regression test in `marketplace.test.ts` runs `Promise.all([DELETE :id, POST install])` and asserts the DB end state matches the response codes (no torn state). **[medium] F3 — Legacy `DELETE /marketplace/:key/uninstall` is now bulk-destructive:** the endpoint, kept for backwards compat in this cycle's first pass, would soft-delete EVERY active install of a key in one call — a multi-install footgun. **Fix:** handler replaced with `reply.code(410).send({ error: { code: 'ENDPOINT_REMOVED', message: '…use DELETE /v1/integrations/:id…' } })` — no DB writes, clear migration message. **Tests:** 5 new regression cases in `marketplace.test.ts` (F1: partial unique index enforces uniqueness, different-name siblings allowed, non-locked rows ignored; F2: concurrent delete+install race; F3: legacy endpoint returns 410 with the seed install untouched). Backend suite 83 → 88 green. `apps/api` typecheck/build clean. Review notes in `prompts/results/REVIEW_4-A_MARKETPLACE_FIX.md`.
- **Cycle 4-A — Marketplace fix: catalog vs. instances, multi-install, SFTP separation (2026-05-12).** First cycle of Batch 4. Sebastian's Batch 3 production review uncovered three related bugs in the Marketplace surface; this cycle fixes all three at the API contract level. **(1) Catalog contamination:** `GET /v1/integrations/marketplace/catalog` previously used `Map<key, singleRow>` and emitted `name: row?.name ?? entry.name`, so an installed instance with a custom name ("Shopify Test") overwrote the static catalog name. **Fix:** `Map<key, row[]>` groups all installs per key, response always uses `entry.name` + `entry.description` from `MARKETPLACE_CATALOG`, instance names live in the new `installations[]` array. The response shape changes from `{ installed: boolean, integrationId, isEnabled, installedAt }` to `{ installCount: number, installations: Array<{integrationId, instanceName, isEnabled, installedAt}> }`. **(2) Multi-install blocked:** the install handler's `findFirst` conflict check + `P2002` race-catch returned `409 ALREADY_INSTALLED` when any active row existed for the marketplace key. Combined with the v6b partial unique index `integrations_tenant_marketplace_key_active_unique`, this enforced a one-install-per-key rule that broke "two Shopify shops" / "two Hive warehouses" use cases. **Fix:** both 409 paths removed; install endpoint creates a fresh integration row on every call. The locked-mapping-template branch is now idempotent — re-installing the same key reuses existing templates (skips creation) so two installs of `shopify` share one set of locked templates per `(tenant, marketplaceKey)`. **Partial unique index dropped** via `apps/api/src/db/sql/unique-active-marketplace-integration.sql` (rewritten from `CREATE UNIQUE INDEX` to `DROP INDEX IF EXISTS` — idempotent, runs through the existing manual-migration sweep on every deploy). **(3) SFTP in marketplace:** the `sftp` entry sat in `MARKETPLACE_CATALOG` under category `'erp'` so it surfaced alongside Shopify/Xentral in the marketplace UI, even though SFTP has its own dedicated page at `/integrations/automatic`. **Fix:** `MARKETPLACE_CATALOG` no longer contains SFTP; it moved to a new `INTERNAL_INTEGRATIONS` constant in the same file. `getCatalogEntry(key)` now searches both arrays so the install endpoint still resolves `sftp` (the setup wizard wires through `/integrations/marketplace/sftp/install`), but the public GET catalog response excludes it. **Per-instance uninstall:** new `DELETE /v1/integrations/:id` soft-deletes one installation by ID. Locked mapping templates are torn down ONLY when this was the last surviving install of the marketplace key — siblings keep the shared templates. The old `DELETE /marketplace/:key/uninstall` is preserved for backwards compatibility (deprecated, no caller in the web app anymore). **Frontend (R4):** `MarketplaceCatalogEntry` reshaped to match the new response. `useUninstallIntegration` now takes `integrationId` (string) instead of marketplace key. New `useIntegrationsList(type?)` hook for `GET /integrations` — needed because `/integrations/automatic` previously read SFTP rows off the catalog payload, which no longer carries them. The marketplace page (`/integrations/marketplace/page.tsx`) flattens `catalog[*].installations[*]` into one card per installation; cards show the instance name as the title with the catalog name as subtitle. The app-store modal always shows an "Install" button (no more disabled "Already Installed" state) plus an `installCount` badge ("2× installed") when prior installs exist. The setup wizard no longer reads SFTP install state from the catalog — multi-install means every wizard run creates a fresh SFTP integration row, the "already installed" amber-banner path is gone, and the in-wizard directory browser is retired (it never had an integrationId during the wizard flow under multi-install). **Tests:** 6 new cases in `apps/api/src/routes/integrations/__tests__/marketplace.test.ts` — SFTP exclusion from catalog, static name preservation under custom instance name, multi-install of the same key surfaces `installCount: 2`, locked-template idempotency on re-install, per-instance DELETE leaves siblings intact, cross-tenant DELETE returns 404 without existence leak. Backend suite 77 → 83 green. `apps/api` typecheck/build green; `apps/web` typecheck/build/lint green (0 errors; pre-existing warnings unchanged).
- **Cycle 3-E Codex stop-time review (2026-05-08) — all 3 findings ACTIONABLE, fixed in-session.** Codex's adversarial review of `origin/main...HEAD` flagged three real correctness/data-integrity issues. **[high] Schedule queue/DB silent desync** — `POST/PATCH/DELETE/TOGGLE` on `apps/api/src/routes/integrations/schedules.ts` previously persisted DB state and then attempted BullMQ scheduler sync in a best-effort `try/catch` that only logged on failure. A Redis outage left the operator with a green schedule that would never fire (or a "deleted" schedule that kept firing). **Fix:** all four routes now sync the BullMQ scheduler **first** and return `503 SCHEDULER_UNAVAILABLE` on failure with the DB unchanged; POST generates the schedule UUID upfront via `randomUUID()` so the scheduler can be registered before the row exists, and on the rare DB-insert-after-queue-success failure the scheduler is best-effort torn down. **[high] Schedule timezone not persisted** — POST accepted `timezone` and used it for cron preview/registration, but the value lived only in the request scope. PATCH/TOGGLE re-registered with the route default (`Europe/Berlin`), silently shifting any non-Berlin schedule. **Fix:** new `timezone String @default("Europe/Berlin")` column on `IntegrationSchedule` (migration `20260508130000_review_3e_fixes`), IANA validation via `Intl.DateTimeFormat`, PATCH treats the persisted column as source of truth, TOGGLE reads `existing.timezone` for re-registration. `shapeSchedule()` no longer takes a timezone argument — it pulls from the row. **[medium] `import_runs` missing FKs** — `schedule_id` and `credential_id` columns existed but had no FK constraints. Application-level orphan-prevention wasn't enforced at the DB layer. **Fix:** same migration adds two FK constraints with `ON DELETE SET NULL` (chosen over `RESTRICT` so audit-trail rows survive a credential hard-delete or schedule soft-delete — failed-run forensics still reachable). Prisma schema gains the `schedule`/`credential` relations on `ImportRun` and the inverse `importRuns ImportRun[]` arrays on the parent models. **Tests:** 8 new regression cases in `schedules.test.ts` (3 atomicity-503 paths, 2 timezone persistence + IANA validation, 3 FK enforcement including SET NULL on credential delete). Backend suite 69 → 77 green. `apps/api` typecheck/build green; `apps/web` typecheck green (the frontend hook already typed `timezone: string`). Review notes in `prompts/results/REVIEW_3-E_INTEGRATION_UI.md`.
- **Cycle 3-E — SFTP/FTP Integration UI live (2026-05-08).** Final cycle of Batch 3; no more backend cycles in this batch. Replaces the `/integrations/automatic` "coming soon" placeholder with a real card list of installed SFTP/FTP integrations (filtered off `marketplaceKey ∈ {sftp, ftp, ftps}`). Per-integration config page at `/integrations/automatic/[id]` renders six sections in cards: header (name + protocol badge + health-status dot + enable/disable Switch), health summary (last successful sync, last error, consecutive failures), credentials (`CredentialSelector` + `ConnectionTestButton` reuse), directory browser, mapping-template selector, schedule builder + active toggle + next-run timestamp + delete, and the import-runs history at the bottom. **5-step setup wizard** (`SetupWizard`) walks operators through Connection (name + credentials, with inline `CredentialForm` + connection test), Directory (browser, optional file pick), Mapping (template selector defaulting to column-name matching), Schedule (with "manual only" opt-out), and Summary review. On submit it installs the `sftp` marketplace entry if not already installed, then creates the schedule via inline `fetch` (the `useCreateSchedule` hook needs the integrationId at instantiation; the wizard learns it post-install — refactor noted in result file). The marketplace app-store modal now opens the wizard for `sftp`/`ftp`/`ftps` keys instead of the generic `MarketplaceInstallDialog`. **New hooks:** `use-credentials`, `use-schedules`, `use-import` — TanStack Query mirrors of Cycle 3-B/3-C/3-D backend endpoints. **New components:** `connection-test-button.tsx` (idle → testing → success/error with 5 s auto-reset), `credential-form.tsx` (Name/Protocol-radio/Host/Port/User/Password/RemotePath, default-port auto-toggle), `credential-selector.tsx` (Radix Select + inline "New credentials" form), `directory-browser.tsx` (editable path + Browse button + CSV-only file table, 30 s `staleTime` no focus refetch — every call opens an SFTP socket), `mapping-template-selector.tsx`, `schedule-builder.tsx` (multi-step with locale-aware preview, presets ≥60 min auto-convert to `interval_hours` to satisfy the backend's `intervalValue ≤ 59` cap), `import-runs-table.tsx` (paginated + status badges + "Import now" dialog wrapping `useImportNow`). **Backend:** one additive change in `apps/api/src/lib/marketplace-catalog.ts` adding the `sftp` entry (single entry covering SFTP/FTP/FTPS — protocol picked at install time). New `useIntegration(id)` hook in `use-integrations.ts` for `GET /v1/integrations/:id`. Full `integrations.sftp.*` i18n block in en + de (~150 keys). `pnpm -C apps/web typecheck` + `build` + `lint` (0 errors) green; `pnpm -C apps/api typecheck` + `build` green.
- **Cycle 3-D Codex stop-time review (2026-05-08) — both [high] findings ACTIONABLE, fixed in-session.** Codex stop-time review of the Cycle 3-D feature commit caught two real correctness issues. **[high] PATCH schedule update bypassed credential/template validation:** `PATCH /v1/integrations/:id/schedules/:scheduleId` accepted `credentialId` and `csvMappingTemplateId` from the body and wrote them to the row without any of the gates POST enforces — an admin could swap in a credential bound to a different integration (cross-attribution: schedule fires against integration A's connector with B's credentials), an inactive credential, a credential from another tenant (existence enumeration via 404 vs 409), or a template with `direction='export'` / `resourceType='products'` (worker would either run with garbage column mappings or fail-the-cron-tick at the in-worker template-validity guard). **Fix:** extracted `validateCredentialForSchedule` + `validateMappingTemplateForSchedule` helpers in `apps/api/src/routes/integrations/schedules.ts`; both POST and PATCH now route through them. PATCH only runs validation when the body explicitly carries a non-null value for the relevant field (`csvMappingTemplateId: null` clears the template without re-validating; `credentialId` undefined skips the credential gate). **[high] Worker created Incident + bumped consecutiveFailures on every BullMQ retry attempt:** the queue ships with `attempts: 3`, so each transient connection failure produced 3 Incident rows in the operator's dashboard within ~3.5 minutes, snapped `consecutiveFailures` from 0 to 3 in one cron tick, and flipped `healthStatus` straight to `'failing'` on the very first failure. Contradicts DECISIONS 2026-05-07 ("After 3 failures, the job pauses until the next scheduled run. A notification is triggered" — i.e. cron tick is the unit of consecutive failure, not the retry attempt). **Fix:** `queue.ts` worker handler now derives `isFinalAttempt = job.attemptsMade + 1 >= (job.opts.attempts ?? 1)` and threads it into `processSftpImportJob`; `markScheduleAndHealth` gates Incident creation + consecutiveFailures bump + healthStatus escalation on the flag. Non-final attempts still update `lastError`/`lastErrorAt` on the integration and finalize the `ImportRun` (audit-trail completeness) but escape per-attempt churn on the operator-visible state. **Tests:** 5 new PATCH-revalidation cases in `schedules.test.ts` (other-integration credential → 409 mismatch, inactive → 409, cross-tenant → 404, export-template → 400, null-clear-path → 200) plus a new `apps/api/src/jobs/__tests__/sftp-import.worker.test.ts` (3 cases: non-final-attempt asserts no incident + no escalation but `lastError` set; final-attempt asserts incident + `consecutiveFailures=1` + `'degraded'`; 3-attempt simulation asserts exactly 1 incident across the full retry cycle — the production-scenario regression check). Backend test suite 61 → 69 green; typecheck + lint (0 errors) + build all green. Full disposition: `prompts/results/REVIEW_3-D_SCHEDULE_ENGINE.md`.
- **Cycle 3-D — Schedule Engine (2026-05-08).** Third backend cycle of Batch 3. Wires BullMQ "job schedulers" (the v5 replacement for repeatable jobs) to the existing `IntegrationSchedule` table, ships full schedule CRUD (admin-only) under `/v1/integrations/:id/schedules…`, and adds a queue-fed worker that drains the `sftp-import` queue by streaming the latest remote CSV through the Cycle 3-C import pipeline. **R1 (queue + worker bootstrap):** `apps/api/src/jobs/queue.ts` gains `SFTP_IMPORT_QUEUE_NAME`/`SFTP_IMPORT_JOB_NAME` constants and a `sftpImportQueue` `new Queue('sftp-import', …)` with `attempts: 3` + `backoff: { type: 'exponential', delay: 30_000 }` defaults. `startSftpImportWorker()` lazily `import()`s the worker module so the test bundle never loads it; `index.ts` calls it after `app.listen` resolves, gated on `config.NODE_ENV !== 'test'`. Redis client keeps `lazyConnect: true` so import-time is fine; if Redis is unreachable when the worker boots, the function logs and returns `null` — API stays up, schedules persist in Postgres, and re-register on the next worker boot. **R2 (cron utils):** `apps/api/src/lib/cron-utils.ts` exports `buildCronExpression(scheduleInput)` (interval_minutes → `*/N * * * *`, interval_hours → `0 */N * * *`, daily → `MM HH * * *`, weekly → `MM HH * * D1,D2,...`; ISO Sunday `7` translated to cron `0`), `describeCron(cron, locale)` (en + de rendering), and `nextRunTimes(cron, count, tz)` (drives `nextRunAt` via `cron-parser`'s `CronExpressionParser.parse(cron, { tz }).next().toDate()`). New dep: `cron-parser@^5.5.0`. **R3 (schedule CRUD):** new `apps/api/src/routes/integrations/schedules.ts` plugin under `/v1`, gated by `authMiddleware` + `tenantMiddleware` + `requireRole('admin')`. `GET /integrations/:id/schedules` returns rows with `cronDescription` (en) + `cronDescriptionDe` + `nextRunAt`. `POST` accepts `{ name, resourceType: 'stock', direction: 'import', scheduleType, intervalValue?, timeOfDay?, weekdays?, credentialId, csvMappingTemplateId?, timezone? }`; Zod superRefine mirrors the `schedule-check-constraints.sql` field-combination rules; the server compiles the cron via `buildCronExpression` (never accepts cron from client) and registers a BullMQ scheduler via `sftpImportQueue.upsertJobScheduler(scheduleId, { pattern, tz }, { name: 'sftp-import', data: { scheduleId } })`. Credential is gated on the same binding/active levers the import route uses (404 not-found, 409 `CREDENTIAL_INTEGRATION_MISMATCH`, 409 `CREDENTIAL_INACTIVE`); template (when provided) must be `direction='import'` + `resourceType='stock'`. `PATCH /:scheduleId` recomputes the cron from any structured-field change and re-registers the scheduler (or removes it when `isActive=false`). `DELETE /:scheduleId` soft-deletes (`deletedAt = now()` + `isActive = false`) and removes the scheduler. `PATCH /:scheduleId/toggle` flips `isActive` and registers/removes the scheduler. The schedule UUID doubles as the `jobSchedulerId` so create/update/toggle/delete are stable + idempotent. **Schedule timezone is currently a default, not a column** — `IntegrationSchedule` has no `timezone` column; body accepts it (default `'Europe/Berlin'`) and threads it into `nextRunTimes` + `upsertJobScheduler`, but it is NOT persisted on the row. Tracked in KNOWN_TODOS. **R4 (worker):** new `apps/api/src/jobs/sftp-import.worker.ts`. Job entry `processSftpImportJob({ scheduleId })`. Loads the schedule + integration + credential + mapping template; gates on the operational levers (`schedule.deletedAt!==null` || `!isActive` || `integration.deletedAt!==null` || `!integration.isEnabled` || `credential.deletedAt!==null` || `!credential.isActive` || unsupported credentialType || incomplete creds → skip with warn log). Creates an `ImportRun` shell with `trigger: 'scheduled'`, `scheduleId`, `credentialId`. Lists the credential's `remotePath`, picks the newest `.csv`, streams via the Cycle 3-C SFTP/FTP wrappers, buffers up to 5 MB, decodes via `decodeBuffer`, parses via `parseCsvStreamingBuffer` (max 10k rows), and runs `processStockImportRows` with `source: 'sftp'`/`'ftp'` (FTPS rolls up under `'ftp'`). Finalises the run with `status` ('failed' if every row errored or zero rows processed; 'partial' if some rows errored; 'success' otherwise) + stats + `errorSummary`. Worker runs outside `tenantMiddleware` so RLS is bypassed (Prisma connects as the table owner, same posture as the test harness — DECISIONS 2026-05-02); every query scopes by `schedule.tenantId` explicitly. **Health policy:** success → `healthStatus='healthy'` + `consecutiveFailures=0` + `lastSuccessfulSyncAt=now()`; partial → `'degraded'` + no consecutiveFailures change; failed → `consecutiveFailures += 1`, then `'failing'` if it reaches 3 else `'degraded'`, plus an `Incident` row (`sourceType='job'`, `severity='error'`, `code='SFTP_IMPORT_FAILED'`, `isUserVisible=true`) on every failed run. Connector throws are re-thrown so BullMQ counts the attempt as failed and applies the queue's exponential backoff (3 attempts, 30 s/60 s/120 s); row-level errors do NOT retry. **Tests:** new `apps/api/src/routes/integrations/__tests__/schedules.test.ts` (19 tests). 7 `buildCronExpression` cases (the four schedule types, ISO Sun → cron 0, throw-on-missing-intervalValue, throw-on-missing-weekdays). 4 `describeCron` cases (en + de pairs). 6 route validation/auth cases (happy-path daily create with scheduler-call assertion, missing intervalValue 400, missing weekdays 400, invalid timeOfDay 400, viewer-403, inactive-credential 409). Lifecycle case (create → list → update intervalValue → verify cron changed + scheduler re-registered → toggle off → verify scheduler removed → toggle on → verify re-registered → delete → verify soft-delete + scheduler removed). Cross-tenant case (tenant B cannot list/patch/delete tenant A's schedule). The tests `vi.mock('../../../jobs/queue.js', …)` wholesale so route handlers exercise their wiring without opening Redis; the worker module is intentionally untested at the unit level — its internals are exercised by the Cycle 3-C import-pipeline tests. Backend suite 42 → 61 green; `pnpm -C apps/api typecheck` + `lint` (0 errors) + `build` green. Full disposition: `prompts/results/RESULT_3-D_SCHEDULE_ENGINE.md`.
- **Cycle 3-C Codex review (2026-05-08) — both [high] findings ACTIONABLE, fixed in-session; one [medium] deferred.** `/codex:adversarial-review --base origin/main` returned three findings against the 32-file Cycle 3-B-backlog + 3-C diff. **[high] Credential/integration binding not enforced:** `loadIntegrationAndCredential` validated the integration and the credential independently by tenant but never compared `credential.integrationId` against the path's `:id`. A bound credential (DECISIONS 2026-05-07 supports both bound and reusable credentials) could be used against any other integration of the same tenant — wrong attribution on `import_runs.integration_id`, wrong source data into the wrong connector. Fix: explicit binding check in `apps/api/src/routes/integrations/sftp-import.ts` — if `credential.integrationId !== null && credential.integrationId !== integration.id` → 409 `CREDENTIAL_INTEGRATION_MISMATCH`. Reusable credentials (`integrationId === null`) explicitly remain accepted across integrations of the same tenant. **[high] Imports run with disabled integration / inactive credential:** the preamble selected only `id` from the integration (`isEnabled` was never read) and never checked `credential.isActive`. Manual import could proceed even when an operator had disabled the connector or deactivated the credential — defeats the operator's "halt all syncs" lever during incident response or rollback. Fix: `select` now includes `isEnabled`; two new 409 gates fire before any remote call or `ImportRun` shell creation — `INTEGRATION_DISABLED` when `integration.isEnabled === false`, `CREDENTIAL_INACTIVE` when `credential.isActive === false`. The 409 status matches the existing `CREDENTIAL_IN_USE` 409 on the credential vault — operational-state conflicts on this codebase are 409. `integration.status` (sync-health indicator) is intentionally not gated on; the two ACTIONABLE gates Codex called out are the explicit operator-controlled levers. **[medium] Import failures returned as HTTP 200 → DEFERRED** per DECISIONS 2026-04-16 (operational-ergonomics, not security/correctness): the route's always-200 contract is a deliberate design choice — the `ImportRun` record carries `status`, the Cycle 3-D worker is in-process so it doesn't hit the HTTP endpoint, the Cycle 3-E UI consumes `data.status` directly, and the existing CSV multipart upload route returns 200 with row-level errors as body fields. Symmetric semantics across both routes is more valuable than aligning to monitor/retry conventions when no external automation hits the endpoint. Tracked in `KNOWN_TODOS.md` so a future cycle that wires external automation can flip the contract deliberately. **Tests:** 4 new in `apps/api/src/routes/integrations/__tests__/sftp-import.test.ts` — credential/integration mismatch → 409 + connector-not-called + no-run-row, reusable credential (integrationId=null) accepted, integration-disabled → 409 on both routes + no run row, credential-inactive → 409. Backend test suite 38 → 42 green; `pnpm -C apps/api typecheck` + `lint` (0 errors) green. Full disposition: `prompts/results/REVIEW_3-C_SFTP_CONNECTOR.md`.
- **Cycle 3-C — SFTP/FTP Connector + Import Pipeline (2026-05-08).** Second backend cycle of Batch 3. Adds the connector primitives the SFTP/FTP automated-import feature needs, ships three admin-only endpoints, introduces the `import_runs` history table, and flips the credential DELETE verb from soft-delete to hard-delete (DECISIONS 2026-05-07). **R0 (credential hard-delete):** `DELETE /v1/credentials/:id` now calls `tx.integrationCredential.delete(...)` inside the existing SERIALIZABLE transaction; the 409 `CREDENTIAL_IN_USE` guard, the P2034 single-retry, and the 503 fallback all stay — only the write verb changed. Two test assertions flipped from "deletedAt not null" to "row is null"; the 503 + 409 path assertions keep the `deletedAt: null` shape because those paths don't actually delete. **R1+R2 (connector primitives):** `apps/api/src/integrations/sftp-client.ts` exports `listSftpDirectory(config, path, filter?)` (sorts by `modifiedAt` desc, optional extension filter, 30s outer `withTimeout`) and `streamSftpFile(config, path)` returning `{ stream, cleanup }` so the caller manages lifetime. `ftp-client.ts` mirrors the shape via `listFtpDirectory` and `streamFtpFile`; the FTP variant streams via `PassThrough` because basic-ftp's `downloadTo` writes to a Writable. The shared `SftpFileInfo` and `SftpStreamHandle` types live in `sftp-client.ts`. Listing falls back to `new Date()` for FTP servers that don't speak MLSD. **R3 (`import_runs`):** new Prisma model + migration (`20260508120000_add_import_runs/migration.sql`) + RLS file (`apps/api/src/db/sql/rls-policies-v5.sql` — registered in `run-manual-migrations.ts`). Tenant-scoped, FK→tenants and FK→integrations, indexed on `(tenantId)` and `(tenantId, integrationId)`. Status taxonomy: `running` → `success` | `partial` | `failed`. Test truncate list updated. **Shared core:** `apps/api/src/services/stock-import/process-csv-stock.ts` ports the row-loop from `routes/csv/index.ts` (resolve variant → resolve location → resolve stockType → resolve bin → resolve batch → `upsertStockLevel`); exports `STOCK_IMPORT_FIELDS`, `buildStockExtractor`, `parseCsvStreamingBuffer`, `decodeBuffer`, `processStockImportRows`. `csv/index.ts` is intentionally not refactored to use it (kept out of the prompt's Modified scope; bounded duplication tracked in KNOWN_TODOS). **Routes (`apps/api/src/routes/integrations/sftp-import.ts`):** single Fastify plugin, `authMiddleware` + `tenantMiddleware` + `requireRole('admin')` as global preHandlers. `GET /v1/integrations/:id/files?credentialId=...&path=...` lists CSVs from the remote dir (path defaults to credential's remotePath, then `/`). `POST /v1/integrations/:id/import-now` body `{ credentialId, filePath?, mappingTemplateId? }` creates an `ImportRun` shell, streams the remote CSV → 5 MB-capped buffer → existing parser → `processStockImportRows`, finalises the run with stats + status, always returns 200 with the run record (operator checks `data.status`). Connection failures → `status: 'failed'` with the inner error message in `errorSummary`. `stock_movements.source` is `'sftp'` for SFTP credentials, `'ftp'` for FTP/FTPS credentials. `GET /v1/integrations/:id/runs?page&perPage` returns paginated history newest-first with `{ data, meta: { total, page, perPage } }`. **Tests:** new `apps/api/src/routes/integrations/__tests__/sftp-import.test.ts` (9 tests): R0 hard-delete pin, CSV-only listing, viewer-403, SFTP end-to-end with source assertion, FTP variant routing, connector-throw → failed, no-filePath newest-CSV picker, runs pagination, cross-tenant 404. Backend suite 29 → 38 tests all green. `pnpm -C apps/api typecheck` + `build` + `lint` (0 errors) green. **Notion update deferred** — no Notion MCP in this Claude Code session; flipping Cycle 3-C to ✅ Ausgeführt + linking the result file is queued for Sebastian / Claude (Chat).
- **Cycle 3-B Codex review (2026-05-07) — both findings ACTIONABLE, fixed in-session.** `/codex:adversarial-review --base origin/main` returned two `[high]` findings against the 23-file Cycle 3-B diff. **[high] Credential routes lack role-based authorization:** `credentialsRoutes` only ran `authMiddleware` + `tenantMiddleware`; the auth middleware falls back to `'viewer'` for users without an explicit role claim, so non-admins could create/delete credentials and use the SFTP/FTP connection tests as an outbound-network proxy. Trust-boundary break flagged as no-ship. Fix: added `requireRole('admin')` (existing `apps/api/src/middleware/require-role.ts` helper, used elsewhere in `tenantRoutes`) as a third global `preHandler` on the `credentialsRoutes` plugin so every route — GET list, POST create, POST unsaved-test, PATCH, DELETE, POST saved-test — gates on admin role. Added 7 negative tests via `it.each` (every method × route returns `403 FORBIDDEN` for `role: 'viewer'`) plus a separate case proving `role: 'manager'` is also rejected. **[high] DELETE non-atomic check-then-act race:** the `count()` of active schedules and the soft-delete `update()` lived as two separate statements outside a transaction, so a concurrent INSERT into `integration_schedules` could land between them and leave a soft-deleted credential referenced by an active schedule. Fix: wrapped the count + update inside a single `request.db.$transaction(async (tx) => {…}, { isolationLevel: Prisma.TransactionIsolationLevel.Serializable })`. Postgres' SERIALIZABLE detects the conflicting read-set/write-set across concurrent transactions and aborts one with `serialization_failure` (Prisma `P2034`). Route retries once; if the retry also raises P2034, surfaces `503 SERIALIZATION_FAILED` so the client can retry. Three new tests cover the spy-driven retry path: first `$transaction` call raises a real `Prisma.PrismaClientKnownRequestError({ code: 'P2034' })`, second call commits → 200 + actual soft-delete; both calls raise P2034 → 503 + credential row stays intact; regression pin that the in-use 409 still fires from inside the new transaction. Backend test suite 19 → 29 tests, all green. Full disposition: `prompts/results/REVIEW_3-B_CREDENTIAL_VAULT.md`. Follow-up tracked in KNOWN_TODOS: when Cycle 3-D builds the schedule-create handler, that handler must atomically validate the referenced credential is not soft-deleted (the symmetric race).
- **Cycle 3-B — Credential Vault Backend (2026-05-07).** First Batch-3 backend cycle — opens the SFTP/FTP automated-import feature. **Schema:** `IntegrationCredential.integrationId` is now nullable (`String?`); migration `20260507120000_credential_integration_optional` runs `ALTER TABLE integration_credentials ALTER COLUMN integration_id DROP NOT NULL`. `credentialType`'s comment now lists `'ftps'` alongside the existing types. Schema-v3 RLS policy already covered `integration_credentials` so no new RLS file. **Encryption:** new `apps/api/src/lib/encryption.ts` ships `encrypt`/`decrypt` (AES-256-GCM, 12-byte IV, 16-byte auth tag, wire format `iv_hex:authTag_hex:ciphertext_hex`) plus `encryptCredential`/`decryptCredential` convenience pair reading the key from `config.CREDENTIALS_ENCRYPTION_KEY`, and a `MASKED_SECRET = '••••••••'` constant. `deriveKey` re-validates the 64-hex / 32-byte shape on every call (belt-and-suspenders against future per-tenant or rotated keys). **Connectors:** new `apps/api/src/integrations/{sftp,ftp}-client.ts` thin wrappers around `ssh2-sftp-client@12.1.1` and `basic-ftp@6.0.1`; `apps/api/src/integrations/connection-utils.ts` carries `withTimeout` (10-second outer guard via `Promise.race`), `sanitizeConnectionError` (newline-strip + 200-char cap + canned `'Connection failed'` fallback), and the shared `ConnectionTestResult` type. Both wrappers funnel all errors through the sanitizer so stack traces / internal paths never leak to API responses. **Routes** (`apps/api/src/routes/credentials/index.ts`, registered in `server.ts` under `/v1`): `GET /v1/credentials` (list with masked sensitive fields + `usageCount` from a `groupBy` against active `IntegrationSchedule` rows referencing the credential); `POST /v1/credentials/test` (registered before the parametric path — tests unsaved bodies, no DB I/O); `POST /v1/credentials` (Zod-validated body, defaults port to 22 for sftp / 21 for ftp+ftps when omitted, encrypts `password`/`token`/`secret` before write, validates `integrationId` belongs to tenant when provided); `PATCH /v1/credentials/:id` (tri-state `null`/string/omitted contract for sensitive fields — `null` clears, string re-encrypts, omitted keeps existing ciphertext); `DELETE /v1/credentials/:id` (soft-delete via `deletedAt = now()` + `isActive = false`; rejects with `409 CREDENTIAL_IN_USE` when an active schedule references the credential); `POST /v1/credentials/:id/test` (decrypts password server-side, dispatches to SFTP or FTP connector based on `credentialType`, bumps `lastVerifiedAt` only on success). The route narrows untrusted-string `credentialType` via a runtime `isTestableType` predicate before connector dispatch, so future schema-extending types (`api_key`/`oauth`/`webhook`/`basic_auth`) can coexist in the same table without an SFTP-as-default footgun. **Tests** (`apps/api/src/routes/credentials/__tests__/credentials.test.ts`, 6 new): CRUD round-trip with decrypt-from-DB assertion (encrypted blob is `iv:tag:ciphertext`, decrypts back to `'supersecret'`); `null`-clears-password + string-sets-token PATCH semantics; `409 CREDENTIAL_IN_USE` when an `IntegrationSchedule` row references the credential; cross-tenant 404s on PATCH/DELETE/test (no existence enumeration); SFTP saved-test success → `lastVerifiedAt` bumped + connector called with the **decrypted** password; SFTP saved-test failure → response carries the connector's error string + `lastVerifiedAt` stays `null`; unsaved FTPS test routes through `testFtpConnection({ ..., secure: true })` and writes nothing to DB. `vi.mock('../../../integrations/sftp-client.js', …)` swaps the wrappers for `vi.fn()` so no socket opens. Backend test suite 13 → 19 green; `pnpm -C apps/api typecheck` + `build` + `lint` (0 errors) green.
- **Cycle 3-A — UX Polish (2026-05-06).** First Batch-3 cycle, frontend-only, no backend touch. Four isolated polish items from Sebastian's Batch-2 production review. **R1 (variant highlight):** the selected variant row on the product detail page (`apps/web/src/app/(dashboard)/products/[id]/page.tsx`) flipped from `bg-brand-100` + `shadow-[inset_4px_0_0_0_#0d9488]` (teal/teal — invisible against the brand-tinted page) to `bg-blue-50` + `shadow-[inset_4px_0_0_0_theme(colors.blue.500)]`. Selection is now obvious at a glance even on single-variant products (which gained the highlight in Cycle 2-F). All click/keyboard/`aria-selected` wiring unchanged. **R2 (filter labels):** `SingleFilter` in `apps/web/src/components/stock/movement-filters.tsx` no longer collapses to "Alle" whenever the selected `Set` happens to equal the option count. Logic now: `'all'` sentinel → "Alle"; concrete `Set` with 1–3 entries → comma-joined names ("Lager: Hauptlager, Nebenlager"); 0 or ≥ 4 entries → `t('selectedCount', { count })`. The Select-all button already wrote the `'all'` sentinel so it correctly returns the label to "Alle" — no change needed there. Pristine-entry deep links from the stock list (single matching warehouse / bin / type in the data) now show the actual name in the trigger instead of misleading "Alle" copy. **R3 (Lagerplatz column):** `apps/web/src/components/stock/stock-movement-table.tsx` gained a new column between Lager and Typ rendering `storageLocationName ?? '—'`. The field was already on `StockMovementRow`; no hook/backend change. New i18n key `stockMovements.columns.storageLocation` ("Storage location" / "Lagerplatz") in en + de. **R4 (sidebar identity card):** desktop expanded sidebar (`apps/web/src/components/shared/sidebar.tsx`) and mobile sidebar (`mobile-sidebar.tsx`) restructured the identity block. Outer `flex items-center justify-between` row now wraps a vertical column of org name + user display name on the left; the `PlanBadge` is the right-side child of the centred row, so it visually sits between the two text lines as a single compact identity card instead of floating on the first line. Collapsed-desktop unchanged. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS unchanged at 111 kB; `/products/[id]` 5.33 → 5.34 kB.
- **Cycle 2-F Codex review (2026-05-06) — both findings ACTIONABLE, fixed in-session.** `/codex:adversarial-review --base origin/main` returned two correctness findings against the 19-file Batch-2-tail diff. **[high] Chart fetch broadened in 2-F but still capped:** `chartFilters` dropped `locationId`/`stockType` from the API call so dropdowns could populate from the full dataset, but kept `CHART_PER_PAGE = 200` + `sortDir: 'desc'`. On a high-throughput tenant with many warehouses × bins × types, the latest 200 rows could be dominated by high-velocity series — silently excluding the user's pre-selected line even when matching movements existed in-range. Fix: hybrid fetch driven by a new `pristineEntry` boolean. While the user is on the page with legacy single-value URL params present AND no filter dropdown touched yet, `chartFilters` includes the legacy trio (locationId/storageLocationId/stockType) so the API narrows the response to the entry-point scope and the selected series is guaranteed within the cap. As soon as any filter handler fires (`handleLocationsChange` / `handleStorageLocationsChange` / `handleStockTypesChange`), `pristineEntry` flips to `false` and the chart fetch broadens — restoring the dropdown-population goal. **[medium] `storageLocationId` deep-link contract incomplete:** 2-F added `?storageLocationId=…` to the stock-list link for chart pre-selection but skipped backend Zod, the frontend filter type, and `tableFilters` — the table consequently ignored the bin scope. Fix: full end-to-end wire-up. `apps/api/src/routes/stock/index.ts` `movementsQuerySchema` gained `storageLocationId: uuidSchema.optional()` + a one-line `where.storageLocationId` predicate. `apps/web/src/lib/api/use-stock-movements.ts` `StockMovementsFilters` exposes the new dimension. `tableFilters` now includes `storageLocationId: legacyStorageLocationId` so the table narrows to the entry-point bin. New backend test `filters by storageLocationId (Cycle 2-F review fix)` seeds two bins + one bin-agnostic + two bin-scoped movements, asserts the filter returns exactly the bin-A row. Backend movements suite 4 → 5 tests, all green. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS unchanged at 111 kB. Full disposition: `prompts/results/REVIEW_2-F_FINAL_POLISH.md`.
- **Cycle 2-F — Final polish (2026-05-06).** Closing-cycle frontend polish for Batch 2; no backend touch. **R1+R2 (filters + single-line removal):** the `hasSingleLineFilters`/`isProductMode` distinction is gone; `/stock/movements` always renders the multi-line layout with three cascading filters. New `selectedStorageLocations` state + URL param `?storageLocations=` joins the existing `?locations=` and `?stockTypes=`. Series key extended from `locationId|stockType` to `locationId|storageLocationId|stockType` (bin-agnostic rows use a `-` sentinel). `MovementFilters` component now exposes three `<SingleFilter>` triggers; the `pruneStorageForLocations` helper inside `handleLocationsChange` auto-deselects child bins when their parent warehouse is deselected (synchronous-in-handler pattern, not an effect, to avoid URL/state ping-pong). Bin filter mirrors the stock page's `matchesStorageLocation` logic — bin-agnostic series pass through when `selectedStorageLocations === 'all'`, hidden when an explicit Set is set. Stock-list Bewegungen link extended to forward `storageLocationId` (when the row carries one) so the cascade pre-selects too. Chart fetch always runs the broad query (no per-row filter at the API) so dropdowns can populate from the full dataset. New `chart.multiLine.seriesLabelWithBin` ICU template renders `Lager · Lagerplatz · Bestandstyp`. **R3 (single-variant highlight):** `aria-selected` and the brand-100 + inset teal rail now apply regardless of `variants.length`; `tabIndex`/`role="button"`/click handlers stay gated on multi-variant. Single-variant products see the highlight on the lone row from first paint. **R4 (X-axis):** the window-spread heuristic in `stock-movement-chart.tsx` is replaced with a same-day-duplicate detector — always shows DD.MM., appends HH:mm only when at least two visible points share a calendar day. `minTickGap` swings 32 → 64 in the longer-label case to keep ticks from overlapping. `selectedRangeMs` prop kept on the discriminated union for backwards-compat but no longer consulted. **R5 (`HorizontalScrollFade`):** gradient `w-8` → `w-14`, `via-background/80` → `via-background/95`, plus a `ChevronRight` icon centered in the fade band. Same opacity-fade behaviour driven by `ResizeObserver` + scroll listener. **R6 (content padding):** `PageHeader` and every dashboard page-level wrapper (sticky breadcrumb, toolbar, section, content body) bumped `px-6` → `px-6 md:px-8`. Inner card placeholders (rounded-md border with `px-6` inside the empty-state graphic) intentionally left alone. `<DataTable>` callers on `/products` and `/stock` got a wrapping `<div className="px-6 md:px-8 py-2">` so first-column cells align with the toolbar above (prior 16px misalignment between toolbar and table cells is also fixed). Removed obsolete i18n keys `chart.filtersRequired.{title,description}` and `chart.multiLine.{title,description}`; replaced with `chart.missingScope.{title,description}` (only shown when neither productId nor variantId is in the URL). Added `stockMovements.filters.storageLocationLabel` in en + de. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS unchanged at 111 kB; `/products/[id]` 5.28 → 5.33 kB.
- **Cycle 2-E Codex review (2026-05-06) — one ACTIONABLE high fixed in-session, one medium deferred.** `/codex:adversarial-review --base origin/main` returned two findings against the 20-file diff. **[high] `Clear all` filter state lost via URL/state mismatch:** `selectionToParam(new Set())` wrote `?locations=` but `parseFilterParam('')` collapsed empty-string back to the `'all'` sentinel, so the URL-sync `useEffect` immediately overwrote the user's explicit deselection — the noFilterSelected empty-state branch was effectively unreachable after the first re-render. Fix: made `parseFilterParam` symmetric (`raw === ''` → `new Set()`; malformed `parts.length === 0` → also `new Set()` so a degenerate URL doesn't restart the same ping-pong via a different path). The `raw === null` (param absent) path still returns `'all'` or seeds from the legacy `locationId`/`stockType` fallback at mount. **[medium] Filter options derived from truncated 200-row chart sample → DEFERRED** per DECISIONS 2026-04-16: the prompt explicitly scoped filter logic to "the already-fetched movements data", so deriving options from `chartRows` is the documented design. Existing `truncatedNotice` (Cycle 2-B-FIX) surfaces the partial-data condition; structural fix is server-side downsampling, already tracked in the existing "stock_movements chart aggregation/downsampling" KNOWN_TODOS entry. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS unchanged at 111 kB. Full disposition: `prompts/results/REVIEW_2-E_MOVEMENTS_FILTERS_LEGEND.md`.
- **Cycle 2-E — Movements filters & legend rework (2026-05-06).** Frontend-only follow-up to Cycle 2-C. Two new multi-select filter dropdowns (Lager + Bestandstyp) live above the chart in multi-line / `productId`-mode and a legend click now **fades** the corresponding line to ~20% opacity instead of removing it from the chart. **R1+R2 (filters):** new `apps/web/src/components/stock/movement-filters.tsx` exposes a `MovementFilters` shell composed of two `DropdownMenu` + `DropdownMenuCheckboxItem` triggers — using the existing shadcn primitive avoids pulling in `@radix-ui/react-popover` + `cmdk` for the Popover/Command pattern. Trigger label reads `Lager: Alle` when `selection === 'all'` (sentinel distinct from "every option in the set" so URL writeback can drop the param entirely) or `Lager: N ausgewählt` once the user touches it. Each menu carries Select-all/Clear-all footer buttons; `onSelect={(e) => e.preventDefault()}` keeps the menu open after each tick (Radix doesn't ship that for free). Distinct location options are derived from `chartRows` via a `Map<locationId, locationName>`; stock-type options come from a plain `Set<string>`. Filtering of `productSeries` happens client-side by splitting the existing `${locationId}|${stockType}` composite series key. **R3 (legend):** `stock-movement-chart.tsx` swapped its `hidden: Set<string>` state for `faded: Set<string>` and dropped the `<Area hide>` prop — faded series stay rendered with `strokeOpacity={0.2}` plus reduced `dot`/`activeDot` opacity, and the legend formatter renders the label in muted gray when faded. Replaced the default `<Tooltip formatter>` with a custom `content` render so faded entries can be muted in the tooltip too (gray text + 0.45 opacity row); single-line tooltip uses the same content path with one row, visually identical to before. **R4 (pre-selection):** filter state is seeded once at mount — explicit `?locations=`/`?stockTypes=` win, otherwise (in multi-line entry only) fall back to the legacy `?locationId=`/`?stockType=` URL params so links from the stock list pre-select the matching location/type. Subsequent URL syncs read only the explicit multi-select params — the `useEffect` deps suppress the legacy fallback so a clean URL won't accidentally re-seed back to the legacy value. **R5 (URL):** `?locations=loc-id-1,loc-id-2`, `?stockTypes=available,reserved`, written via `router.replace` (no history pollution, matches Cycle 2-B date-range pattern). Empty/missing param → `'all'`. **R6 (i18n):** new `stockMovements.filters.{locationLabel,stockTypeLabel,all,selectedCount,selectAll,clearAll,noOptions}` + `stockMovements.chart.noFilterSelected.{title,description}` in en + de; updated `chart.multiLine.legendToggleHint` to describe the new fade-vs-remove split. **Chart fetch change in multi-line mode:** `chartFilters` now drops `locationId`/`stockType` (only `productId`/`variantId` + range remain) so the dropdown options reflect the full product dataset; single-line mode (URL trio) keeps the API call narrowed to the trio (no behaviour change). Table fetch (`tableFilters`) is intentionally unchanged per non-goal — it still consumes all URL params, so a multi-line entry from `?productId=X&locationId=Y` shows the table narrowed to Y while the chart filter can broaden to other locations (transient chart/table mismatch — accepted MVP friction, tracked in KNOWN_TODOS). `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS 110 → 111 kB.
- **Cycle 2-D — Production review fixes (2026-05-05).** Six purely visual/UX fixes from Sebastian's production review of Batch 2. (R1) `PageHeader` (`apps/web/src/components/shared/page-header.tsx`) is now `sticky top-0 z-20 bg-background` by default, with a new `noSticky` opt-out for the one page that already wraps its own sticky region. The four breadcrumb-bar pages (`products/[id]`, `products/import`, `stock/import`, `stock/movements`) had their inline breadcrumb wrappers promoted to `sticky top-0 z-20 bg-background`; `stock/movements` is the only page with both breadcrumb + PageHeader, so its two layers got wrapped in a single sticky parent and the inner PageHeader uses `noSticky` to avoid both layers fighting for top-0. Result: breadcrumb + back button + page title stay pinned on every dashboard page during scroll. (R2) Variant-row highlight on the product detail page wasn't visible on production because `border-l-4` on a `<tr>` is dropped under Tailwind's default `border-collapse: collapse`. Replaced with a `shadow-[inset_4px_0_0_0_#0d9488]` inset box-shadow (paints reliably regardless of collapse mode), and bumped the selected-row tint from `bg-brand-50` → `bg-brand-100` for clearly visible contrast. (R3) Movements chart tick formatter (`stock-movement-chart.tsx`) was deciding granularity from the data spread, so a 30d preset that happened to contain only same-day movements collapsed to `HH:mm` ticks with no date. New optional `selectedRangeMs?: number` prop on `StockMovementChart` (added to both branches of the `{movements} | {series}` discriminated union); the movements page computes `to - from` from the selected range and passes it; the chart prefers `selectedRangeMs` over the data-spread fallback. Custom same-day ranges still hit the `≤ ONE_DAY_MS` HH:mm branch since `from` (00:00:00.000) → `to` (23:59:59.999) ≈ 1d - 1ms. (R4) "Bewegungen anzeigen" button moved out of the product-detail page header (next to Edit/Delete) and down next to the "Aktueller Bestand" / "Stock" section heading. Now variant-aware: link is `/stock/movements?productId={id}&variantId={selectedVariantId}` when a variant is selected, else `?productId={id}` only. The selected variant is the one the variant-table currently highlights, so toggling rows updates the link without a re-render dance. (R5) New reusable `HorizontalScrollFade` (`apps/web/src/components/shared/horizontal-scroll-fade.tsx`) wraps content that may overflow horizontally and paints a right-edge gradient that fades in/out based on scroll position via `ResizeObserver` + scroll listener. Applied to `ProductStockTable` (the table inside the Quick-View Sheet); the wrapper preserves the existing `rounded-md border border-border` shell so visuals are unchanged outside narrow containers. (R6) New `useAuthUser` hook (`apps/web/src/lib/api/use-auth-user.ts`) wraps `supabase.auth.getUser()` in a TanStack query so the sidebar identity block doesn't re-call it on every mount; new `PlanBadge` component (`apps/web/src/components/shared/plan-badge.tsx`) renders the tenant's `plan` as a compact pill (muted gray for `trial`, brand-50 → 100 → 600 escalation through `starter`/`growth`/`enterprise`). Both desktop sidebar and mobile sidebar now show org name (prominent) + plan badge (right-aligned, compact) on one line and the user's display name (composed from `firstName/lastName` user-metadata, fallback to `fullName`, fallback to email) below in muted secondary text; collapsed-desktop state shows nothing (existing behaviour). New i18n keys `nav.plans.{trial,starter,growth,enterprise}` in en + de. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS unchanged at ~110 kB; `/products/[id]` 4.89 → 5.28 kB.
- **Cycle 2-C Codex review (2026-05-05) — both findings ACTIONABLE, fixed in-session.** `/codex:adversarial-review --base origin/main` returned two medium findings, both targeting movement-page time-range code that originated in Cycle 2-B / 2-B-FIX (not 2-C itself, but in the cumulative `develop`-vs-`main` diff). **[medium] `rangeForPreset` off by one:** `subDays(startOfDay(now), days)` paired with `endOfDay(now)` made the 7d preset span 8 calendar days, 30d → 31, etc. — the chart/table windows were systematically wider than the labels promised. Fix: `subDays(startOfDay(now), days - 1)` so each preset includes exactly the labelled count of days inclusive of today. **[medium] Unvalidated URL `from`/`to`:** raw query strings were forwarded verbatim into state and onward into `useStockMovements`, so a malformed deep link could fire 4xx responses through TanStack's retry. Fix: a single `parseUrlRange(rawFrom, rawTo)` helper validates each value with `Number.isFinite(new Date(raw).getTime())`, enforces `from <= to`, and round-trips through `new Date(ms).toISOString()` for normalized equality. All three URL→state sites (initial `useState`, mount effect, URL-sync effect) now route through the helper; invalid input falls back to the stable default-30d range AND rewrites the URL so the address bar matches state. `pnpm -C apps/web typecheck` + `build` green; bundle sizes unchanged. Full review disposition + boundary verification: `prompts/results/REVIEW_2-C_MOVEMENTS_FROM_PRODUCT.md`.
- **Cycle 2-C — Movements von Produktseite (2026-05-05).** Adds a second entry point into `/stock/movements`. The product detail page (`apps/web/src/app/(dashboard)/products/[id]/page.tsx`) now carries a `BarChart3` outlined button next to Edit/Delete labelled "View movements / Bewegungen anzeigen" (reuses the existing `stock.viewMovements` key) that links to `/stock/movements?productId={id}`. The movements page split chart-gating into `hasSingleLineFilters` (existing variant+location+stockType trio) and `isProductMode` (productId only); product mode fans the flat `useStockMovements` payload out into one series per `${locationId}|${stockType}` composite key, sorted by display name (`${locationName} · ${stockType}`). `StockMovementChart` was refactored to a discriminated-union prop (`{ movements }` xor `{ series }`) so the existing single-line caller stays unchanged in behaviour and visual styling (teal gradient fill, smart time-tick formatter); multi-line mode renders one `<Area>` per series with `fill="transparent"` (avoids the eight-overlapping-fills mud), a stroke colour from an 8-hue Tailwind-500 palette (blue/emerald/amber/rose/violet/cyan/orange/slate), and a paired `strokeDasharray` (`'0'`/`'6 3'`/`'2 3'`/`'8 2 2 2'`/`'10 4'`/`'1 3'`/`'4 2 1 2'`/`'5 5'`) so colourblind operators can still distinguish adjacent series; `<Legend>`'s `onClick` toggles a local `Set<string>` of hidden series keys via Recharts' `hide` prop (visible series in the tooltip stay in sync). `connectNulls` + `isAnimationActive={false}` + `dot={{ r: 2 }}` in multi-mode keeps sparse per-series rows readable and prevents jitter on legend toggle. Tooltip `formatter` swaps the raw series key for the human label. The 2-B-FIX truncation notice still applies (counts total rows across all series). New i18n keys `stockMovements.chart.multiLine.{title,description,legendToggleHint,seriesLabel}` (ICU `{location}`/`{stockType}` on `seriesLabel`) in en + de. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS 109 → 110 kB; `/products/[id]` 4.89 kB.
- **Cycle 2-B-FIX — Codex pass-2 findings fixed (2026-05-05, commit `83ae51f`).** A second Codex adversarial review run with `--base origin/main` (after pass 1 caught only unrelated workflow churn) flagged two real correctness issues in `apps/web/src/app/(dashboard)/stock/movements/page.tsx`. Both fixed in this commit. **[high] Chart silent truncation:** the chart's `useStockMovements` filter ran with `sortDir: 'asc'` + `perPage: 200`, so a tenant with >200 movements in the selected range would see the EARLIEST 200 — the most recent stock activity silently fell off the chart. Fix: flipped to `sortDir: 'desc'` (chart component already sorts ascending internally before render so visual order is unchanged), and when `chartData.meta.total > chartRows.length` an amber `<p role="status">` notice now renders above the chart with shown/total counts and a hint to narrow the range. New i18n key `stockMovements.chart.truncatedNotice` with `{shown}`/`{total}` ICU placeholders in en + de. **[medium] Range/URL desync:** `range` was initialized from `search` once via `useState` and never re-synced; browser back/forward or on-page deep-links could leave queries running on stale `from`/`to` while the address bar showed something else. Fix: a stable `defaultRangeRef` (useRef) holds the mount-time 30d ISOs so URL→state→URL round-trips don't drift on milliseconds; a one-time mount-effect writes the default 30d range into the URL when URL is empty (so URL is always the single source of truth from first render onward — intentionally changes the prior "URL stays clean for default" behavior); and a sync effect keyed on `search` updates `range` whenever the URL changes externally (URL-empty resets to the stable default; other URL changes update range and clear `activePreset` since we cannot infer preset choice from a URL alone — URL→preset rehydration stays a tracked TODO). `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS 108 → 109 kB. Full review disposition + reasoning: `prompts/results/REVIEW_2-B_MOVEMENTS_CHART_POLISH.md` (Pass 2 section).
- **Cycle 2-B Codex adversarial review (2026-05-05) — pass 1 disposition: both findings deferred (governance, not code).** Review run via `/codex:adversarial-review` with default scope (working tree), which at review time held only the unrelated WORKFLOW.md + PROMPT_TEMPLATE.md churn from Claude Chat — not the cycle's pushed feature commits. Codex returned [high] on `PROMPT_TEMPLATE.md` and [medium] on `WORKFLOW.md`, both objecting to the post-push review model. Per the findings policy (DECISIONS 2026-04-16) only Security / Data Integrity / Correctness issues get fixed; both findings are governance/process and argue against a deliberate, documented decision (DECISIONS 2026-05-05 — "Codex review decoupled from Claude Code plugin"). No code change. Findings recorded in a new "Workflow" section of `KNOWN_TODOS.md` along with the scope-mismatch follow-up (post-push reviews need an explicit `--base origin/main` until the planned GitHub Action automates the trigger). Full disposition + reasoning: `prompts/results/REVIEW_2-B_MOVEMENTS_CHART_POLISH.md`.
- **Cycle 2-B — Movements chart polish (2026-05-05).** Frontend-only follow-up to Cycle 1-E. The `/stock/movements` page now carries a date-range control row above the chart: four preset buttons (`7d`/`14d`/`30d`/`90d` in en, `7T`/`14T`/`30T`/`90T` in de) on the left and a pair of native `<input type="date">` controls (from–to) on the right. Active preset uses the `default` Button variant; picking a custom date clears the highlight. Range writes to the URL via `router.replace` as `?from=ISO&to=ISO`; on page load, URL params win, otherwise the page defaults to last-30-days (URL stays clean for the default). Range threads into both `chartFilters` and `tableFilters`; `useStockMovements` already accepted `from`/`to` so no hook change was needed (the filters object is already part of the query key). The Recharts X-axis on `stock-movement-chart.tsx` switched from a string `dateLabel` to `type="number"` + `scale="time"` + `domain=['dataMin','dataMax']` keyed off epoch-ms; the tick formatter picks granularity from the visible data range (≤24h → `HH:mm`, ≤7d → `DD.MM. HH:mm`, otherwise `DD.MM.`), `minTickGap=32` keeps ticks legible, and the Tooltip's `labelFormatter` always renders the full local datetime regardless of axis granularity. Same-day entries no longer collapse into a single X-position — each point gets its own. Empty-state copy splits: with a range set, chart shows `chart.emptyRange.{title,description}` ("No movements in this time range" / "Keine Bewegungen in diesem Zeitraum"); without, the existing "No movements yet" message stays. Date inputs convert to/from ISO at the operator's local-day boundaries (`from` → local 00:00:00.000, `to` → local 23:59:59.999) so a "today" pick on the right input includes movements through end-of-today. Pagination resets to page 1 on every range change. New i18n under `stockMovements.presets.{7d,14d,30d,90d}`, `rangeLabel`, `fromLabel`/`toLabel` (sr-only), and `chart.emptyRange` in both locale JSONs. No backend touch, no schema change. `pnpm -C apps/web typecheck` + `build` green; `/stock/movements` first-load JS at 306 kB.
- **Cycle 2-A — UI Micro-Fixes (2026-05-05).** First Batch 2 cycle. Three isolated frontend changes, no backend touch. (R1+R2) Variant table on the product detail page is non-interactive for single-variant products: when `product.variants.length === 1`, the row no longer renders `cursor-pointer`, `aria-selected`, `tabIndex`, `role="button"`, or the click/key handlers — the row stays a static visual entry while `selectedVariantId` still defaults to that variant so `ProductStockTable` keeps its `variantId` prop. For multi-variant products, every row now reserves a transparent `border-l-4` and the selected row switches to `border-l-brand-600` plus a solid `bg-brand-50` (was the near-invisible `bg-brand-50/60`); the contrast is obvious on white. (R3) The stock-list movements-link icon switched from `Activity` (heartbeat) to `BarChart3` (`apps/web/src/app/(dashboard)/stock/page.tsx`); the icon was the only `Activity` usage in the web tree. (R4) Removed the placeholder `Bestandswert` / `Stock value` column from `ProductStockTable` (rendered `—` on every row because `stock_levels`/`product_variants` carry no cost field) — both the column header and the cell renderer are gone, the orphan i18n key `products.detail.stockValue` was dropped from `en.json` + `de.json`. The Quick-View Sheet inherits the change automatically (it composes `ProductStockTable`). Underlying TODO (cost field + valuation method → real Bestandswert numbers) stays tracked in KNOWN_TODOS / NEXT.md backlog item 13. Build + typecheck green.
- **Fix — Restore 400 (2026-05-05).** `POST /v1/products/:id/restore` from the web app was returning 400 instead of restoring. Root cause: `apiFetch` (and `apiFetchWithMeta`) in `apps/web/src/lib/api/client.ts` set `Content-Type: application/json` on every request, even when no body was being sent. Fastify's default JSON parser then rejects the empty body with `FST_ERR_CTP_EMPTY_JSON_BODY` ("Body cannot be empty when content-type is set to 'application/json'") on methods that run the body parser (POST/PUT/PATCH). DELETE callers were unaffected because Fastify skips the body parser on DELETE; GETs harmlessly carried a no-op Content-Type. Both fetch helpers now only advertise the JSON content type when `options.body` is defined. Backend route, route tests, and the restore mutation hook are all unchanged. The 4 backend tests in `apps/api/src/routes/products/__tests__/restore.test.ts` were passing because `app.inject` without `payload` doesn't set a Content-Type header — they exercised the route correctly but never reproduced the frontend's "Content-Type set + body empty" shape.
- **Cycle E — Bestands-Verlauf + Chargen-Liste (2026-05-05).** Closes the A–E frontend-review batch. Backend: `GET /v1/stock/movements` (the route already existed but only carried `variantId/locationId/movementType/from/to/page/perPage`) gained `productId`, `stockType`, and `sortDir` filters, lifted `perPage` cap from 100 → 200 (default 50), and now returns a denormalized payload — `variantSku`, `productId`, `productName`, `locationName`, `storageLocationName`, `batchNumber`, `quantity` (alias of `quantityAfter`), `quantityBefore`, `delta`, `movementType`, `source`, `createdAt`. Tenant scoping unchanged (`where.tenantId`). Tests at `apps/api/src/routes/stock/__tests__/movements.test.ts`: empty-state envelope, variantId-filter + from/to date-range, pagination (`perPage=2` over 3 rows → page 1 has 2, page 2 has 1, total 3), and cross-tenant RLS isolation. 12 backend tests now green (4 new + 8 prior). Frontend: dedicated `/stock/movements` page (not a sheet — see DECISIONS) renders a Recharts area chart (top — only when `variantId` + `locationId` + `stockType` are all present in the URL) plus a paginated movement-history table (bottom — Datum, Produkt, Variante (SKU), Lager, Typ, Menge, Änderung (color-coded delta), Quelle). Pagination controls drive `page` state; sort toggle flips `sortDir` between `asc`/`desc`. New hook `useStockMovements` in `apps/web/src/lib/api/use-stock-movements.ts` returns `{ data, meta }` via a new `apiFetchWithMeta` helper added to `client.ts`. The unused `useStockMovements` export that lived in `use-stock.ts` was removed (zero callers). The Activity icon on the stock list (placeholder from Cycle B) is enabled and navigates to `/stock/movements?variantId=…&locationId=…&stockType=…`. New `ProductBatchList` component on the product detail page below the stock table (only mounted when `product.batchTracking === true`) — sources its rows from `useStock({ productId, variantId })` filtered to entries with a `batchId`, rendering Charge / MHD / Lager / Menge / Bestandstyp. New top-level `stockMovements` i18n block + new `products.batchList` block + new `products.detail.batchListTitle` key in en + de. Build green, all typechecks green.
- **Cycle D — Produkte-Liste Polish + Soft-Delete Restore (2026-05-04).** New backend endpoint `POST /v1/products/:id/restore` undoes a soft-delete: restores the product (`deletedAt = null`, `deletedBy = null`) plus any variants whose `deletedAt` matches the product's `deletedAt` (the cascade snapshot), so variants that had been soft-deleted independently *before* the product deletion stay deleted. 404 with code `PRODUCT_NOT_FOUND` is returned for both "already active" and "wrong tenant" so existence is never leaked. `GET /v1/products` query param renamed `showDeleted` → `includeDeleted`; semantics flipped from "deleted-only view" to "merge active + deleted in one list" (the new toggle UX). Backend tests live at `apps/api/src/routes/products/__tests__/restore.test.ts`: happy-path with cascade-variant restore (and explicit assertion that an early-deleted variant is left alone), already-active 404, cross-tenant 404 (RLS isolation, including verifying tenant A's product stays deleted), and the `includeDeleted` query-param contract. Eight backend tests now green (4 new + 4 prior). Frontend (`apps/web/src/app/(dashboard)/products/page.tsx`): leftmost actions-column icon is now `Eye` linking to `/products/:id` on every row; deleted rows render a "Gelöscht / Deleted" badge + line-through name; a `RotateCcw` restore button replaces the edit + delete pair on deleted rows; bulk-select checkbox is hidden (with an inert spacer) for deleted rows so they can't slip into bulk-delete; the toolbar toggle now switches the list into merged-view (active + deleted) instead of replacing the view. New `RestoreProductDialog` mirrors the existing delete-confirm shape; new `useRestoreProduct` mutation hook invalidates the products cache. New i18n keys in en + de under `products.deletedBadge`, `products.viewDetails`, `products.restore`, and the full `products.restoreConfirm.*` block.
- **Cycle C — Produkt-Detailseite Refactor (2026-05-04, commits `c592907` + `c14eea2`).** Two commits. **R0 (CI fix, `c592907`):** `apps/api/src/db/migrations/` was gitignored from the initial scaffold; locally Prisma found the migrations because the directory existed in the working tree, but CI checkouts had an empty migrations folder so `prisma migrate deploy --schema src/db/schema.prisma` did nothing and `run-manual-migrations.ts` then crashed on missing tables. Fix: dropped the line from `.gitignore` and committed the five existing migration directories + `migration_lock.toml` (886 lines added, no content change). All 4 backend tests stay green. **R1–R5 (frontend, `c14eea2`):** product detail header (`apps/web/src/app/(dashboard)/products/[id]/page.tsx`) now shows only name, description, unit, and the batch-tracked indicator (R1 — SKU/EAN/Barcode removed; they live in the variant table). Source icon removed from the header (R2). Edit/Delete are labeled `<Button>` (`outline` + `destructive`) replacing the prior icon-only `<button>` taps (R3). New "Quelle / Source" column on the variant table; `ProductSourceIcons` learned an explicit `source` prop so the table can render per-variant icons — currently passes the product-level source per row because `ProductVariant` has no metadata/source column yet (R4, KNOWN_TODOS tracks the schema-side follow-up). Variant rows are clickable (`tabIndex` + Enter/Space keyboard support, `aria-selected`, brand-tinted highlight); `useState<string | null>(selectedVariantId)` plus a `useEffect` defaults to the first variant id once the product loads, and operator clicks override the default. `ProductStockTable` accepts an optional `variantId?` and forwards it to the existing `useStock({ productId, variantId })` filter — backend already supported this; no API change. Integrations section stays a placeholder (variant-reactive wiring deferred per prompt non-goal). Cleaned up two unused i18n keys (`products.detail.editProduct`, `products.detail.deleteProduct`) in en + de.
- **Cycle B — Bestände polish + identical-qty movement write (2026-05-04, commit `fc5036a`).** `upsertStockLevel` extracted from `apps/api/src/routes/csv/index.ts` into `apps/api/src/services/stock/upsert-stock-level.ts`; the inline `if (currentQty.equals(newQty)) return 'skipped'` short-circuit is gone. New outcome type `'created' | 'updated' | 'unchanged'` — identical-quantity calls now bump `last_synced_at` on the level row and append a `stock_movements` row with `delta=0`. Caller in `csv/index.ts` rolls `'unchanged'` into the existing `result.updated` counter so the public CSV import response shape stays `{created, updated, skipped}` (no frontend break; `skipped` is now reserved for the dry-run-missing-location case). `isUniqueViolation` lifted to a new `apps/api/src/lib/db-errors.ts` so both csv and stock paths share the predicate without back-references. **First test under the Cycle TH harness:** `apps/api/src/services/stock/__tests__/upsert-stock-level.test.ts` — two cases (identical-quantity → two movement rows; non-zero delta → outcome `'updated'`). All four backend tests green (smoke + new). Frontend: stock list (`stock/page.tsx`) splits the combined "Charge (MHD)" column into separate `Charge` + `MHD` columns; three-dot dropdown collapsed into a disabled `Activity` icon button next to the existing Quick-View Eye button (target: Cycle E movement view); `ManualAdjustDialog` component, the `useUpsertStock` hook, and the entire `stock.adjust` + `stock.manualAdjust` i18n blocks (en+de) are gone. Quick-View `ProductStockTable` gained a `Bestandswert`/`Stock value` column rendering `—` (no cost field on `stock_levels`/`product_variants` yet — KNOWN_TODOS now tracks the cost-data dependency). Backend `PUT /stock` endpoint (manual adjust) is intentionally untouched per prompt scope.
- **Test-Harness Foundation shipped (2026-05-02).** Backend now has a Vitest harness against Postgres 16 in Docker (`apps/api/docker-compose.test.yml`, port 5433, named volume + healthcheck). `apps/api/.env.test` carries every var `config.ts` requires (incl. `SUPABASE_WEBHOOK_SECRET`, 64-hex `CREDENTIALS_ENCRYPTION_KEY`). `vitest.config.ts` runs sequentially (`pool: 'forks'`, `singleFork: true`) with dotenv loading `.env.test` before any test imports config. `src/test/global-setup.ts` runs `prisma migrate deploy` + the manual-migration runner; refuses to migrate against any DB other than `localhost`/`127.0.0.1:5433`. `src/test/setup.ts` truncates all 25 tenant-scoped tables in `beforeEach` (verified against `schema.prisma`). Helpers: `buildTestApp()`, `signTestJwt()` + `authedHeaders()` (real HS256 tokens — no `NODE_ENV==='test'` bypass anywhere in production code), `createTestTenant()`, shared `testDb` Prisma client. Smoke suite `src/test/smoke.test.ts` has two green tests: `GET /v1/health` (no auth, 200) + `GET /v1/products` for a fresh tenant (signed JWT, 200 + empty array). New scripts: `test:up`, `test:down`, `test:setup`. CI gets a `postgres:16-alpine` service container on 5433 and `continue-on-error: true` on the Test step (initial non-blocking window per testing-strategy decision; flip-to-blocking tracked in NEXT.md backlog after 5 cycles).
- **Marketplace install-name render fix (2026-05-02).** `GET /integrations/marketplace/catalog` now selects `Integration.name` and falls back to the static catalog default only when no installed row exists. Persisted custom names entered at install time now appear on the marketplace cards. Two-line read-path fix in `apps/api/src/routes/integrations/index.ts` (added `name: true` to the `select`; changed `name: entry.name` to `name: row?.name ?? entry.name` in the response mapper). Closes Bug #1 of the 2026-04-30 frontend triage.
- All Phase 3A/3B/3C work shipped: auth webhook, tenant provisioning, dashboard, products, stock, integrations skeleton, rules placeholder, notifications placeholder, settings.
- **Phase 4 CSV product import is live** — backend (mapping templates CRUD, preview, import with EAN/SKU matching, dry-run, error report, OOM-safe streaming parser) and frontend (integrations page with two tabs, drag-and-drop upload, mapping template editor with 2-step flow + live preview, `/products/import` route).
- **CSV encoding support is live (2026-04-18).** `iconv-lite` decodes ISO-8859-1 / Windows-1252 / UTF-8 buffers before the streaming parser. Encoding source precedence: template > request field > `'utf-8'` default. Unknown encodings silently fall back to UTF-8 (see KNOWN_TODOS).
- **CSV required-field rules finalized (2026-04-18).** Name + SKU + Barcode required + CSV-column only; Beschreibung optional + no fixed value; Kategorie removed from mapping (still in schema); Einheit optional + fixed value allowed. `batchTracking` renamed to "MHD- / chargengeführt" (DE) / "Best-before / Batch tracked" (EN). Sidebar logo links to `/products`. CSV dialog focus-ring clipping fixed.
- **CSV stock import is live (2026-04-21, 6 fix iterations).** Backend `POST /integrations/csv/import/stock`, `STOCK_IMPORT_FIELDS` + `buildStockExtractor`, variant resolution barcode-first → SKU, `upsertStockLevel` writing `stock_movements` audit rows (movementType `'sync'`), comma-decimal quantities. Frontend: `useImportStock` hook, resourceType-aware `MappingTemplateDialog`, stock tab in `CsvImportPanel`. i18n keys for stock fields in en + de. Last cycle (FIXES6) inverted savepoint cleanup-error precedence and added `extractErrorMessage` helper for cause-chain logging on row-level errors.
- **CSV import surface expanded (post-FIXES6, 2026-04-21):**
  - **Import buttons on toolbars:** Products and Stock list pages each carry an "Import" button in the toolbar; dedicated routes `/products/import` and `/stock/import` host the panel.
  - **Resource-toggle UX:** when the resource type is fixed (route-driven), the toggle is hidden; the panel renders only the active tab.
  - **Stock import auto-create:** unknown storage-location names create the bin on the fly; stock-type fallback to system defaults; Lagerplatz filter on stock page.
  - **Stock page Lager/Lagerplatz columns + filters** and a per-product stock table.
  - **Stock-type system-key precheck:** import no longer creates duplicate tenant-scoped rows for system keys (e.g. `available`, `reserved`); see DECISIONS 2026-04-21 (precheck).
  - **Batch column on stock page** (batch number + locale-formatted expiry); expiry rendered timezone-safe via `YYYY-MM-DD` parsing to avoid off-by-one in negative UTC offsets.
- **CSV import row error reasons sanitized (2026-04-29, commit `5606dd4`).** Both savepoint-cleanup catches (`upsertStockLevel` + stock-type precheck) now throw `AggregateError(stable message, [cleanupErr, insertErr])` instead of embedding `cleanupErr.message` directly. `result.errors[].reason` no longer leaks driver/SQL internals to API callers. Both row-loop catches now also call `request.log.error({ err, row })` so the full cause chain reaches server-side logs via pino's err serializer. Resolves the high-severity Codex finding from the FIXES6 retro review.
- **PROJECT.md refreshed to Phase 4 reality (2026-04-29).** Section 5 `integrations` block gained marketplace + health fields (`is_enabled`, `marketplace_key`, `logo_url`, `category`, `health_status`, `last_successful_sync_at`, `last_error_at`, `consecutive_failures`, `sync_direction`); SQL paths corrected to `apps/api/src/db/sql/`. New "Tables defined in other sections" pointer table maps `external_references`/`csv_mapping_templates`/`integration_credentials`/`integration_schedules`/`notification_templates`/`incidents`/`partners` to their feature sections. Section 6 endpoint list grew CSV (`/csv-mappings*`, `/integrations/csv/*`) + Marketplace (`/integrations/marketplace/*`) clusters, plus `/storage-locations`, `/stock-types`, and `/products/:id/variants*`. Section 7 gained a Marketplace + identity-lock subsection. Section 13 documents auto-runner SQL deployment. Last-updated bumped to 2026-04-29; version → 0.7.0.
- **Marketplace polish 2 (2026-04-29).** Install dialog name is now persisted: `POST /integrations/marketplace/:key/install` accepts an optional `{ name }` body (Zod-validated), resolves to `parsed.data?.name?.trim() || entry.name`, and stores it on `Integration.name`. Marketplace cards expose a `MoreVertical` dropdown → confirm-dialog → `useUninstallIntegration` calling `DELETE /integrations/marketplace/:key/uninstall`. `useToggleIntegration` now passes an `onError` callback that shows a destructive toast with `t('toggleFailed')`; success path stays silent. All three logo render sites (card, install dialog, app-store modal — via a new inline `CatalogLogo` component) fall back to `IntegrationLogoPlaceholder` on `<img>` `onError` instead of vanishing. New i18n keys (`actions`, `uninstall*`, `toggleFailed`) shipped in en + de. PATCH name-immutability for marketplace integrations stays — rename-after-install is a separate cycle. See DECISIONS 2026-04-29 (Marketplace install name is now persisted).
- **Stock-overview navigation polish (2026-04-30).** SKU is now a link to the product detail page; an Eye-Button next to the row dropdown opens a right-side Quick-View sheet (product meta header — SKU, Barcode, Einheit, batch-tracking — plus the existing `ProductStockTable` and a "Zum Produkt" footer link). `GET /stock` response now includes `productId` (Prisma include already loaded `variant.product`). New shadcn `Sheet` primitive at `apps/web/src/components/ui/sheet.tsx` (Radix Dialog under the hood with `cva` slide variants) and a new `StockQuickViewSheet` component. New i18n keys under `stock.quickView` (en + de).
- **CSV row-error sanitization broadened (2026-04-30).** New `apps/api/src/lib/csv-errors.ts` with `VariantNotFoundError`, `InvalidNumberError`, `InvalidDateError`, `StockLevelInvariantError` classes and a `sanitizeRowError` helper. Both per-row catches in `csv/index.ts` now route through the sanitizer; `result.errors[].reason` is whitelisted to actionable strings (variant not found, invalid number, invalid date, P2002 concurrent write, P2000 length, P2025 missing record) or the generic `'Data integrity violation — see server logs'` fallback. Invalid `expiryDate` for batched products is now an explicit row error instead of silent null. UUID-leaking invariant message in `upsertStockLevel` is gone — IDs travel only on the `StockLevelInvariantError` instance, captured server-side via pino's err serializer. The legacy `extractErrorMessage` helper had zero remaining callers and was removed. Closes Codex's broad-scope follow-up to commit `5606dd4`.
- **Product detail page already renders `<ProductStockTable productId={id} />` (corrective entry).** This was in place from an earlier cycle; STATE.md previously didn't reflect it. Confirmed during the 2026-04-30 stock-overview polish pre-flight.
- **Marketplace + App Store is live (2026-04-21).** Page at `/integrations/marketplace` with installed-integration cards (logo, name, category, status badge, enable/disable Switch). "Add integration" opens the App-Store modal — category sidebar (all / shop / erp / wms / fulfiller), search, install button. `MarketplaceInstallDialog` is a generic install shell with a name input and a placeholder settings block (per-integration OAuth/API-key UI is future work). `IntegrationLogoPlaceholder` SVG fallback for entries without a logo. Hooks: `useMarketplaceCatalog`, `useInstallIntegration`, `useToggleIntegration`. New `Badge` UI primitive. **Marketplace polish shipped (commit `c37acca`):** WMS category label + fixed App-Store modal height. Identity-lock for product SKU/EAN: a `LOCKED_SOURCES = {sftp, ftp}` set + any external reference triggers a 409 `PRODUCT_IDENTITY_LOCKED` on PATCH. CSV-imported products are explicitly NOT locked.
- Sidebar is collapsible (desktop) + mobile drawer + responsive top bar.
- Navigation order: Produkte → Bestand → Integrationen → Regeln → Benachrichtigungen → Einstellungen.
- Root URL redirects to `/products` (middleware + page.tsx).
- Registration flow includes Vorname / Nachname / Firmenname.
- Stock types restricted to tenant-relevant types (backend-side filter).

## What's in flight

Batch 4 in progress. Cycles 4-A (Marketplace fix + review), 4-B (movements filter v2), 4-C (SFTP UI polish), and 4-D (movements filter v3 — this session) shipped. The major production-review items from Sebastian's Batch 3 list are now closed: F1 (marketplace contamination), F2 (multi-install blocked), F3 (movements filter), F4 (Integration-hinzufügen button position), F5 (Wizard vs. Direkt-Konfiguration chooser). Remaining Batch 4 candidates: any spillover Codex findings, deferred SFTP functional test against a real server.

## What's uncommitted

Nothing tracked beyond the Cycle 4-D feature change + memory bank update (will be committed before push). Untracked: `test-data/` (Sebastian's local CSV fixtures, gitignored), `prompts/_templates/REVIEW_TEMPLATE.md` (carried from earlier session, not in carry-over whitelist), modified `prompts/_templates/PROMPT_TEMPLATE.md` (same).

## Critical paths

| File | Role |
|------|------|
| `PROJECT.md` | Single source of truth, architecture |
| `apps/api/src/routes/csv/index.ts` | All CSV backend routes + `parseCsvStreaming` + stock-type system-key precheck + sanitized row errors |
| `apps/api/src/services/stock/upsert-stock-level.ts` | Stock-level upsert + paired stock_movements write (always, including delta=0 on identical-quantity) |
| `apps/api/src/lib/csv-errors.ts` | Custom row-error classes + `sanitizeRowError` whitelist for CSV imports |
| `apps/api/src/lib/db-errors.ts` | `isUniqueViolation` predicate shared across CSV + stock services |
| `apps/api/src/lib/encryption.ts` | AES-256-GCM `encrypt`/`decrypt` + `encryptCredential`/`decryptCredential` (key from `config.CREDENTIALS_ENCRYPTION_KEY`) + `MASKED_SECRET` (Cycle 3-B) |
| `apps/api/src/lib/supabase-admin.ts` | Shared `getSupabaseAdmin()` helper |
| `apps/api/src/integrations/connection-utils.ts` | `withTimeout` (10s outer race) + `sanitizeConnectionError` + `ConnectionTimeoutError` shared by SFTP/FTP wrappers (Cycle 3-B) |
| `apps/api/src/integrations/sftp-client.ts` | `testSftpConnection` + `listSftpDirectory` + `streamSftpFile` (Cycle 3-C); shared `SftpFileInfo` and `SftpStreamHandle` types live here |
| `apps/api/src/integrations/ftp-client.ts` | `testFtpConnection` + `listFtpDirectory` + `streamFtpFile` (Cycle 3-C; FTP + FTPS via `secure: boolean`; basic-ftp `downloadTo` teed through `PassThrough` for the stream API) |
| `apps/api/src/routes/credentials/index.ts` | Credential vault CRUD + connection-test endpoints (`/v1/credentials`); masks sensitive fields with `MASKED_SECRET`; `usageCount` from active-schedule `groupBy`; **hard-delete** with `409 CREDENTIAL_IN_USE` guard runs inside a SERIALIZABLE `$transaction` with P2034 retry + `503 SERIALIZATION_FAILED` fallback (Cycle 3-B + Codex review + Cycle 3-C R0). All routes gated on `requireRole('admin')` (Codex review) |
| `apps/api/src/routes/credentials/__tests__/credentials.test.ts` | 16 tests: encryption round-trip + masking + hard-delete-in-use + cross-tenant + SFTP/FTP connector mocking (Cycle 3-B + 3-C R0 update); admin-only `it.each` (6 routes × viewer/manager rejection) + SERIALIZABLE retry (P2034 → 200 after retry, P2034×2 → 503) + transactional 409 regression (Cycle 3-B Codex review) |
| `apps/api/src/routes/integrations/sftp-import.ts` | Cycle 3-C — admin-only Fastify plugin: `GET /integrations/:id/files`, `POST /integrations/:id/import-now`, `GET /integrations/:id/runs`. Streams remote CSV → 5 MB-capped buffer → `processStockImportRows` shared core; tags `stock_movements.source` with `'sftp'`/`'ftp'`; finalises an `ImportRun` row with status `success`/`partial`/`failed` |
| `apps/api/src/services/stock-import/process-csv-stock.ts` | Cycle 3-C shared core: `STOCK_IMPORT_FIELDS`, `buildStockExtractor`, `decodeBuffer`, `parseCsvStreamingBuffer`, `processStockImportRows` (resolve variant → location → stockType → bin → batch → `upsertStockLevel`). The CSV multipart upload route doesn't call this yet — duplication tracked in KNOWN_TODOS |
| `apps/api/src/routes/integrations/__tests__/sftp-import.test.ts` | 9 tests pinning R0 hard-delete + listing CSV-only + viewer-403 + SFTP/FTP end-to-end with `source` assertion + connector-throw → failed run + no-filePath newest-CSV pick + runs pagination + cross-tenant 404 (Cycle 3-C) |
| `apps/api/src/jobs/queue.ts` | BullMQ Queue setup; Cycle 3-D adds `sftpImportQueue` (3 attempts, exp backoff 30s) + `startSftpImportWorker()` (lazy-imports worker module, gated by callers on `NODE_ENV !== 'test'`) |
| `apps/api/src/jobs/sftp-import.worker.ts` | Cycle 3-D out-of-request worker — `processSftpImportJob({ scheduleId })`. Loads schedule + integration + credential, gates on operational levers, streams remote CSV → `processStockImportRows` shared core, finalises ImportRun + schedule.lastRun* + integration health + Incident on failure |
| `apps/api/src/lib/cron-utils.ts` | Cycle 3-D — `buildCronExpression` (interval/daily/weekly → 5-field cron, ISO 7=Sun → cron 0), `describeCron` (en + de), `nextRunTimes` (cron-parser-driven) |
| `apps/api/src/routes/integrations/schedules.ts` | Cycle 3-D — admin-only schedule CRUD plugin (`GET/POST/PATCH/DELETE/toggle`). Compiles cron server-side (never accepts cron from client) and registers BullMQ schedulers via `upsertJobScheduler`/`removeJobScheduler` keyed by the schedule UUID |
| `apps/api/src/routes/integrations/__tests__/schedules.test.ts` | 32 tests pinning cron compilation (7) + describeCron en+de (4) + route validation/auth (6) + PATCH credential/template re-validation (5, Cycle 3-D Codex stop-time review fix) + full lifecycle with BullMQ-call assertions + cross-tenant 404 (Cycle 3-D) + Cycle 3-E review fixes (3 queue/DB-atomicity 503 paths + 2 timezone persistence/validation + 3 import_runs FK enforcement); mocks `'../../../jobs/queue.js'` wholesale so no Redis I/O |
| `apps/api/src/db/migrations/20260508130000_review_3e_fixes/` | Cycle 3-E review fix — adds `timezone` column to `integration_schedules` (default `'Europe/Berlin'`) + FKs for `import_runs.schedule_id` / `credential_id` (both ON DELETE SET NULL) |
| `apps/api/src/jobs/__tests__/sftp-import.worker.test.ts` | 3 tests pinning final-attempt-only Incident + consecutiveFailures gating (Cycle 3-D Codex stop-time review fix): non-final attempt → no incident + no escalation but `lastError` set; final attempt → incident + `consecutiveFailures=1` + `'degraded'`; 3-attempt simulation → exactly 1 incident across the full cycle |
| `apps/api/src/routes/auth/index.ts` | Auth webhook (production-ready) |
| `apps/api/src/db/schema.prisma` | Schema v4 |
| `apps/api/src/db/run-manual-migrations.ts` | Auto-runs SQL + seeds post-deploy |
| `apps/web/src/app/(dashboard)/integrations/page.tsx` | CSV import UI (two tabs) |
| `apps/web/src/app/(dashboard)/products/import/page.tsx` | Products CSV import route |
| `apps/web/src/app/(dashboard)/stock/import/page.tsx` | Stock CSV import route |
| `apps/web/src/components/csv/` | All CSV frontend components |
| `apps/web/src/app/(dashboard)/stock/page.tsx` | Stock list w/ Lager/Lagerplatz/Charge/MHD columns + filters + SKU link + Quick-View Eye + Activity icon (placeholder for Cycle E movement view) |
| `apps/web/src/components/stock/stock-quick-view-sheet.tsx` | Right-side Quick-View sheet for stock list rows |
| `apps/web/src/components/products/product-stock-table.tsx` | Per-product stock table; accepts optional `variantId` filter (Cycle C) — forwards to `useStock` for variant-scoped readout |
| `apps/web/src/app/(dashboard)/products/[id]/page.tsx` | Product detail page — variant table is the steering element (Cycle C); header carries labelled Edit/Delete buttons; "View movements" button now lives next to the "Aktueller Bestand" section heading and threads `selectedVariantId` into the URL (Cycle 2-D); variant-row highlight uses an inset box-shadow + `bg-brand-100` so collapse-mode tables still render the brand-tinted left rail (Cycle 2-D) |
| `apps/web/src/components/shared/plan-badge.tsx` | Compact subscription-plan pill, brand-tinted by tier (Cycle 2-D) |
| `apps/web/src/lib/api/use-auth-user.ts` | TanStack-cached `supabase.auth.getUser()` summary (id + email + display name) for sidebar identity (Cycle 2-D) |
| `apps/web/src/components/products/product-source-icons.tsx` | Source icon renderer; accepts either `metadata` (extracts `metadata.source`) or an explicit `source` prop (per-variant rendering, Cycle C) |
| `apps/api/src/db/migrations/` | Prisma migration directories — now version-controlled (Cycle C R0) |
| `apps/web/src/components/ui/sheet.tsx` | shadcn Sheet primitive (Radix Dialog + cva slide variants) |
| `apps/web/src/components/shared/sidebar.tsx` | Collapsible sidebar |
| `apps/web/src/middleware.ts` | Supabase SSR auth + root → /products |
| `apps/api/docker-compose.test.yml` | Postgres-in-Docker test DB (port 5433) |
| `apps/api/vitest.config.ts` | Vitest harness config — sequential, .env.test, globalSetup |
| `apps/api/src/test/build-app.ts` | `buildTestApp()` factory |
| `apps/api/src/test/auth.ts` | Test JWT signer (`signTestJwt`, `authedHeaders`) |
| `apps/api/src/test/db.ts` | Tenant-table truncate + `createTestTenant` helper |
| `apps/api/src/test/smoke.test.ts` | Harness proof-of-life suite |
| `apps/api/src/services/stock/__tests__/upsert-stock-level.test.ts` | Pins identical-quantity → movement-row behaviour (Cycle B) |
| `apps/api/src/routes/products/__tests__/restore.test.ts` | Pins restore endpoint + `includeDeleted` query-param semantics (Cycle D) |
| `apps/web/src/components/products/restore-product-dialog.tsx` | Restore-confirm dialog (mirrors `DeleteProductDialog`) — Cycle D |
| `apps/web/src/app/(dashboard)/stock/movements/page.tsx` | Bestands-Verlauf page — chart + table + date-range controls (Cycle E + 2-B); always renders multi-line mode now, grouping rows into series by `locationId|storageLocationId|stockType` (Cycle 2-F, replaces the 2-C single/multi distinction); presets are inclusive of today (`days - 1` math) and URL `from`/`to` are validated through `parseUrlRange` before they hit state or the API (Cycle 2-C Codex review); chart fetch is hybrid — narrowed to the legacy URL trio while `pristineEntry` is true (deep-link from the stock list, before any filter touch) and broad once the user touches a filter, so the selected series is guaranteed within the 200-row cap on entry without losing dropdown population after the user broadens (2-F Codex review fix); table reflects all five legacy single-value URL params including storageLocationId (2-F Codex review fix); cascading bin filter pruned synchronously inside `handleLocationsChange` to avoid effect-loop with URL sync (2-F); **filter dropdown options are derived from a SEPARATE `optionsFilters` `useStockMovements` call (no per-row narrowing) so deep-linked single-option entries don't collapse to "Alle" (Cycle 4-B)** |
| `apps/api/src/routes/stock/__tests__/movements.test.ts` | Pins `GET /stock/movements` filters + pagination + RLS isolation (Cycle E + 2-F); 5 tests, including `storageLocationId` filter coverage added in Cycle 2-F review-fix |
| `apps/web/src/components/stock/stock-movement-chart.tsx` | Quantity-over-time area chart fed by `useStockMovements`; discriminated-union prop (`movements` xor `series`) — single-line (Cycle E + 2-B) and multi-line + clickable Legend + Tailwind-500 palette + dasharray patterns (Cycle 2-C); legend click now FADES the line to ~20% opacity instead of hiding (Cycle 2-E), with custom tooltip content so faded entries also render muted; X-axis tick formatter now decides DD.MM. vs DD.MM. HH:mm based on whether the visible data has same-day duplicates, not on the window spread (Cycle 2-F); `selectedRangeMs` prop deprecated but retained on the type for backwards-compat |
| `apps/web/src/components/stock/movement-filters.tsx` | Lager + Lagerplatz + Bestandstyp multi-select dropdowns for the movements chart; `'all'` sentinel + `selectionsEqual` helper for URL-sync equality (Cycle 2-E + 2-F); third filter added in 2-F with cascade scope coming from the page; `handleToggle` no longer auto-collapses to `'all'` on `size === options.length` (Cycle 4-B — root-cause fix for deep-linked dropdowns silently rewriting to the `'all'` sentinel) |
| `apps/web/src/components/shared/horizontal-scroll-fade.tsx` | Reusable wrapper that paints a right-edge fade when wrapped content overflows horizontally (Cycle 2-D); 2-F bumped gradient `w-8` → `w-14`, opacity `via-background/80` → `via-background/95`, added a `ChevronRight` cue inside the fade band |
| `apps/web/src/components/shared/page-header.tsx` | Sticky-by-default page header (`sticky top-0 z-20 bg-background`); accepts `noSticky` for pages that wrap their own sticky region (Cycle 2-D); `px-6` → `px-6 md:px-8` so the header aligns with the wider dashboard content padding (Cycle 2-F) |
| `apps/web/src/components/stock/stock-movement-table.tsx` | Paginated movement-history table (Cycle E) |
| `apps/web/src/components/products/product-batch-list.tsx` | Chargen list on product detail (Cycle E) |
| `apps/web/src/lib/api/use-stock-movements.ts` | `useStockMovements` hook returning `{ data, meta }` for paginated movements (Cycle E) |
| `apps/web/src/lib/api/client.ts` | `apiFetchWithMeta` helper preserves the envelope's `meta` for paginated callers (Cycle E); both helpers omit Content-Type when no body is sent (FIX_RESTORE_400) |
| `apps/web/src/lib/api/use-credentials.ts` | TanStack hooks for `/v1/credentials` CRUD + connection-test (Cycle 3-E) |
| `apps/web/src/lib/api/use-schedules.ts` | TanStack hooks for `/v1/integrations/:id/schedules` CRUD + toggle (Cycle 3-E) |
| `apps/web/src/lib/api/use-import.ts` | TanStack hooks for remote-file listing + `import-now` + `runs` history (Cycle 3-E) |
| `apps/web/src/lib/api/use-integrations.ts` | Marketplace catalog hooks + Cycle 3-E `useIntegration(id)` for the per-integration config page |
| `apps/web/src/components/integrations/connection-test-button.tsx` | Reusable test button: idle → testing → success (5 s auto-reset) → error (Cycle 3-E) |
| `apps/web/src/components/integrations/credential-form.tsx` | Inline create form for SFTP/FTP/FTPS credentials with default-port toggle (Cycle 3-E) |
| `apps/web/src/components/integrations/credential-selector.tsx` | Dropdown of existing credentials + inline "New credentials" form (Cycle 3-E) |
| `apps/web/src/components/integrations/directory-browser.tsx` | Editable path + Browse button + CSV file table; 30 s `staleTime` (Cycle 3-E) |
| `apps/web/src/components/integrations/mapping-template-selector.tsx` | Filtered `useCsvMappings` dropdown + "Default" option (Cycle 3-E) |
| `apps/web/src/components/integrations/schedule-builder.tsx` | Type/details/preview builder; presets ≥60min auto-convert to `interval_hours` (Cycle 3-E) |
| `apps/web/src/components/integrations/import-runs-table.tsx` | Paginated history + "Import now" dialog (Cycle 3-E) |
| `apps/web/src/components/integrations/setup-wizard.tsx` | 5-step modal wizard; install → schedule create via inline fetch (Cycle 3-E) |
| `apps/web/src/app/(dashboard)/integrations/automatic/page.tsx` | Replaces "coming soon" placeholder with installed-SFTP cards + Add button (Cycle 3-E) |
| `apps/web/src/app/(dashboard)/integrations/automatic/[id]/page.tsx` | Per-integration config page (header/credentials/directory/mapping/schedule/history) (Cycle 3-E) |
| `apps/web/src/components/integrations/marketplace-app-store-modal.tsx` | Now opens SetupWizard for `sftp`/`ftp`/`ftps` keys; generic install dialog for the rest (Cycle 3-E) |
| `apps/api/src/lib/marketplace-catalog.ts` | New `sftp` entry covers SFTP/FTP/FTPS as a single marketplace install (Cycle 3-E) |

## Infrastructure

- Frontend: Vercel, auto-deploy from `main`
- Backend: Hetzner VPS at 178.104.175.191, Kamal-deployed
- DB: Supabase Postgres (managed), RLS-enforced
- CI/CD: GitHub Actions, runs `run-manual-migrations.ts` post-deploy automatically — no manual SQL in Supabase needed
