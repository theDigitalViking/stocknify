# RESULT: <same title as the prompt>

**Prompt:** `prompts/PROMPT_<name>.md`
**Notion:** <URL>
**Branch:** <branch name>
**Last commit:** <commit hash + message>
**Date:** <YYYY-MM-DD>

---

## Summary

<2-3 sentences: what was built, what works.>

## Files changed

<Bullet list with one-line descriptions per file.>

## Key decisions made during execution

<Anything not pre-specified in the prompt that needed a call. E.g. "Reused existing INVALID_REQUEST error code instead of adding INVALID_ENCODING because the existing error path already returns a structured field-level message." Explicitly note any deviation from the prompt and why.>

## Skipped or deferred

<Anything not done, with reason. Items here that are tech debt should also be appended to `prompts/_state/KNOWN_TODOS.md`.>

## Tests

<Test command output summary — pass/fail count, any flakes, coverage delta if relevant.>

## Codex review

<If a review was run: command used, findings count by classification, what was fixed vs. deferred. Otherwise: "Not run for this cycle" + reason.>

## Memory Bank updates

- [ ] `STATE.md` updated
- [ ] `KNOWN_TODOS.md` updated (or N/A)
- [ ] Notion entry → ✅ Ausgeführt
- [ ] This result file written
